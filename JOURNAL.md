# JOURNAL

진행 과정의 의사결정과 트레이드오프, 리서치를 시간 순으로 기록한다. 항목마다 날짜와 시각을 남기고, 완성된
결론뿐 아니라 검토했다가 폐기한 대안과 그 이유까지 적는다. AI 활용 내역도 함께 기록한다(무엇을 물었고,
답을 어떻게 수용·수정·거부했는지).

---

## 2026-09-04

### 19:12 · 프로젝트 시작 — 뼈대 커밋과 개발 가이드

첫 커밋 두 개로 저장소를 시작했다. 커밋 1은 "이 커밋만 체크아웃해도 빌드와 실행이 되는 최소
단위"(Gradle 래퍼, 빌드 스크립트, 앱 진입점, 기본 설정, 컨텍스트 로드 테스트), 커밋 2는 개발
가이드(CLAUDE.md)다.

- **기능 코드·문서·패키징은 의도적으로 제외했다**: 이후 브랜치와 PR 단위로 하나씩 쌓아, 설계와 구현의
  과정이 커밋 히스토리에 그대로 드러나게 한다. 저장소가 비어 있어 최초 뼈대만 main에 직접 푸시했고,
  이후부터는 PR 흐름을 따른다.
- **가이드는 일반 원칙만 담았다**: 의존성 방향, 코딩 컨벤션, 동시성·트랜잭션 규칙, 테스트 전략, 문서화
  규율, 완료 기준, Git 워크플로. 도메인·연동처럼 아직 존재하지 않는 영역의 규칙은 해당 기능이 들어올 때
  같은 작업 안에서 가이드에 더한다 — 문서가 코드보다 앞서 나가지 않게 하기 위함이다.

### 19:50 · 과정 기록 규율 확정

JOURNAL 기록을 가이드의 문서화 규율과 완료 기준(DoD)에 필수로 명시했다. 항목마다 날짜와 시각을 남기고,
설계는 폐기한 대안까지, 개발은 부딪힌 문제와 해결 과정까지, 테스트는 전략과 결과를 적는다. AI를 활용한
경우 무엇을 물었고 그 답을 어떻게 수용·수정·거부했는지도 함께 남긴다.

### 20:11 · 기술 스택 논의 ① — Spring Boot 버전

**제안(AI)**: 3.4.13(추천) / 3.5.x / 4.x 세 갈래를 비교했다. 핵심 논거는 위험의 비대칭성 — 이 프로젝트에서
4.x의 실익(프레임워크 수준 API 버저닝, JSpecify 널 안전성 등)은 범위에 거의 영향이 없는 반면, 감수할
위험(외부 연동·회복탄력성 계열 부가 라이브러리의 호환 문제, 막혔을 때 참고 자료 부족)은 곧바로 일정을
잡아먹는다. 외부 연동이 핵심인 프로젝트라 부가 라이브러리 생태계의 검증 여부가 특히 중요하다. 실제로
Spring Initializr가 최근 3.x 생성을 막고 4.x BOM 해석도 깨져 있는 것을 직접 겪어, 4.x 초기 난기류의
방증으로 삼았다.

**결정(수용)**: **Spring Boot 3.4.13 채택.** 4.x는 위 근거로 거부. 3.5.x는 지원 기간의 이점만 있고
프로그래밍 모델이 3.4와 사실상 동일해 얻는 것이 적다는 반론까지 확인한 뒤 채택하지 않았다 — 이 프로젝트의
우선순위는 최신성·지원 기간보다 완성도와 리뷰 환경 재현성이다.

### 20:13 · 기술 스택 논의 ② — JDK 버전

**제안(AI)**: 요구 하한(Java 21+)을 만족하는 실질 후보는 LTS 두 개 — 21과 25. 추천은 21. 결정적 논거는
조합의 정합성으로, Boot 3.4.13 + Kotlin 2.1.20 + Gradle 8.10.2가 전부 JDK 21 중심으로 검증된 묶음이고,
25로 가면 Kotlin·Gradle까지 연쇄 업그레이드가 필요한데 Kotlin 코드베이스에서는 Java 신규 언어 기능의
체감 이득이 없다. 경험적 근거로, 셸 기본 JVM이 25라는 이유만으로 Gradle 실행이
`IllegalArgumentException: 25.0.3`(Kotlin 컴파일러의 버전 파싱 실패)으로 깨지는 것을 당일 직접 겪었다 —
최신 JDK 주변의 마찰이 실재한다는 방증. 반론 조건(순수 Java 코드베이스, 메모리 민감 서비스)도 확인했으나
해당 없음.

**결정(수용)**: **JDK 21 채택**, Gradle toolchain으로 고정. 25는 이득 없는 연쇄 업그레이드 비용으로 거부.
로컬에 상위 JDK가 있어도 무방하지만 Gradle 실행 JVM(`JAVA_HOME`)은 21을 권장한다.

### 20:15 · 기술 스택 논의 ③ — 웹 모델

**제안(AI)**: MVC + WebClient(추천) vs WebFlux 전면 도입. 이 시스템의 특성 — 웹 표면은 검색 엔드포인트
수준으로 단순하고, 난점은 바깥으로 나가는 병렬 호출에 있으며, 영속화는 JPA(블로킹) — 를 놓고 비교했다.
필요한 능력(다수 공급사 병렬 호출, 호출별 타임아웃, 부분 실패 조합)은 WebClient가 제공하는 것이지
WebFlux 서버 모델이 제공하는 것이 아니므로, 논블로킹은 가치를 내는 바깥 호출 구간에만 한정하는 것이
복잡도 대비 정확한 투자라고 제안. WebFlux 전면 도입은 JPA 블로킹 경계 처리(별도 스케줄러 또는 R2DBC
전환)라는 구체적 비용을 얹는 대신, 우리 규모에서 체감되지 않는 처리량 여유만 준다. 반론 성립
조건(초고동시성 + 수 초짜리 외부 응답으로 요청 스레드 고갈이 실제화되는 환경)도 확인했으나 해당 없음.

**결정(수용)**: **MVC + WebClient 채택.** 웹 계층은 MVC(Tomcat)로 두고, 외부 연동 구간만 WebClient
논블로킹으로 다루며 컨트롤러 경계에서 `.block()`으로 값을 모은다. WebFlux 전면 도입은 거부.

### 20:19 · 기술 스택 논의 ④ — DB

**제안(AI)**: H2 인메모리를 추천했다. 근거는 데이터 성격 — 저장 대상이 외부 목록 API에서 언제든 재생성
가능한 매핑뿐이라 휘발성이 약점이 되지 않고, 설치 없이 즉시 실행되어 로컬·리뷰 경험이 가볍다. 반대
급부로 실DB와의 SQL 방언(dialect)·제약 동작 차이는 인정하고, 문제되는 시점에 전환하는 안을 제시했다.

**결정(수정 수용)**: 제안을 수정해 **MySQL 채택**. 영속성이 필요하다는 판단과 함께, H2가 테스트에서는
유효하더라도 dialect가 실DB와 다를 수 있으므로 **테스트도 운영과 동일한 MySQL 버전을 구동해 실행**하는
방향으로 결정했다. 테스트-운영의 엔진·버전 불일치라는 H2의 근본 약점을 처음부터 제거하는 선택이다.
H2는 런타임 DB로는 거부.

이 결정으로 뼈대의 H2 의존성과 데이터소스 설정은 MySQL(드라이버 + 접속 설정)로 바뀌어야 하고, 테스트는
동일 버전 MySQL을 구동하는 방식(Testcontainers 등)이 필요하다 — 구현은 이후 해당 PR에서 진행한다.

### 20:22 · DB 버전 고정 — MySQL 8.4 (LTS)

**제안(AI)**: "테스트도 운영과 동일한 버전"이 원칙이 됐으므로 버전 고정이 곧 계약이다. 8.4(LTS)를 추천 —
8.0은 2026년 4월로 지원이 끝나 신규 고정 대상으로 부적절하고, 9.x는 이노베이션 릴리스라 서버 고정 대상이
아니다. 도커 이미지와 테스트 컨테이너 모두 `mysql:8.4`로 통일한다.

**결정(수용)**: **MySQL 8.4 고정.**

### 20:23 · 기술 스택 논의 ⑤ — 회복탄력성 라이브러리

**제안(AI)**: 외부 호출이 WebClient의 Reactor 체인 안에서 일어나므로, 필요한 능력 세 가지 — 일시적 실패만
골라 하는 재시도, 반복 실패하는 공급사를 끊는 공급사별 서킷 브레이커, 지표 연동 — 를 기준으로 비교했다.
Resilience4j(추천)는 세 가지를 전부 충족하고, 특히 Mono 체인에 그대로 끼워 넣는 전용 연산자를 제공하는
유일한 성숙 선택지다. Spring Retry는 블로킹 메서드 대상 AOP라 리액티브 구간에 부적합해 배제. Reactor 내장
`retryWhen` 최소주의(의존성 절약, 서킷은 나중에)도 대안으로 제시했으나, 공급사 장애가 일상인 도메인 특성상
서킷의 가치가 분명하고 나중에 도입하면 재시도 코드까지 갈아엎게 되는 경로라는 점을 함께 짚었다. Spring
Framework 7의 코어 재시도 어노테이션은 Boot 4 전용이라 3.4 선택과 일관되게 해당 없음.

**결정(수용)**: **Resilience4j 채택** (retry + circuitbreaker, Spring Boot 3 스타터 + Reactor 연동,
버전 명시 고정). Reactor 내장 최소주의는 거부.

### 20:25 · 기술 스택 라운드 마감 — 나머지 항목은 추후 논의

**결정(수용)**: 지표 수집(Micrometer + Prometheus 노출), API 문서화(SpringDoc/Swagger), 테스트
도구(MockWebServer, Testcontainers)는 지금 정하지 않고 **각 기능을 만들 때 논의**하기로 했다 — 문서와
결정이 코드보다 앞서 나가지 않게 하는 원칙과 일관된 선택이다. 오늘 확정된 기반 다섯 가지(Boot 3.4.13,
JDK 21, MVC + WebClient, MySQL 8.4 + 동일 버전 테스트, Resilience4j)로 `docs/TECH_STACK.md`를 갱신한다.

### 21:09 · 도메인 모델 논의 ① — 상품 단위와 표준화 원칙

**제안(AI)**: 상품 단위는 두 공급사가 공통으로 쓰는 구조(`숙소 > 객실 타입`, 요금·재고는 객실 타입
단위)를 그대로 따라 **숙소 × 객실 타입**으로 잡는다 — 우리가 단위를 발명할 여지가 없는 주어진 조건이다.
표준화 원칙은 두 갈래를 비교했다. **교집합 원칙(추천)**은 두 공급사가 공통으로 채울 수 있는 정보만 담아
소비자가 출처를 신경 쓸 필요가 없게 하고, 대가로 한쪽만 주는 상세를 버린다. **합집합 + nullable**은 정보
손실이 없는 대신 필드가 공급사에 따라 비어, 소비자가 결국 공급사별 분기를 하게 되어 통합 모델의 목적이 모델
표면에서 무너진다.

교집합 채택 시 잃는 것을 명시했다 (요구받은 사항):
- **잃지 않는 것**: 숙소·객실 타입 식별자와 이름, 최대 수용 인원, 조식 포함 여부, 통화, **날짜별 잔여
  객실 수**(연박 판정에 필요한 정보는 보존), 기간 전체 세금 포함 총액.
- **잃는 것(전부 A만 제공)**: 날짜별 1박 단가(일별 요금 캘린더·할증 표시 불가, 평균가만 유도 가능),
  세금 분리 금액("세금 별도" 표기·세전 비교 불가), net 가격 정보 자체. B 쪽 탈락분은 상수화되는
  `taxIncluded` 플래그뿐이라 실질 손실 아님.
- 손실이 문제되는 시점(일별 요금 노출, 세전 정산, 일할 환불)의 대비책 두 가지 — 원본 응답 보관, 또는
  원칙의 명시적 변경 — 도 함께 제시. 어느 쪽도 지금 결정을 비가역으로 만들지 않는다.

**결정(수용 + 보강)**: **숙소 × 객실 타입 단위, 교집합 원칙 채택. 필요가 생기면 공통 모델을 명시적으로
변경하는 방식으로 진화시킨다.** 사용자가 확장성 논거를 보강했다 — 지금은 서로 다른 필드가 사실상 한
가지(요금 표현)지만, 공급사가 늘수록 공급사 고유 필드도 늘어나 모든 필드를 다 가져오는 것은 결국
불가능해진다. 교집합만이 공급사 수에 대해 확장 가능한 원칙이다.

### 21:13 · 도메인 모델 논의 ② — 요금 표준화

**제안(AI)**: 두 공급사의 요금 표현이 정면으로 어긋난다 — A는 날짜별 1박 단가에 세금 별도, B는 기간 전체
총액에 세금 포함. 세 갈래를 비교했다. **gross 총액 단일화(추천)**: A의 날짜별 net+세금은 합산하면 gross
총액이 되지만 B의 총액에서 날짜별·세금을 복원하는 것은 정보가 없어 불가능하므로, 두 공급사가 공통으로
도달할 수 있는 지점은 gross 총액뿐이다(교집합 원칙의 직접 적용). **gross + A만 날짜별 nullable 보존**은
①에서 거부한 합집합+nullable의 요금판이라 원칙상 탈락. **net/세금 분리 유지**는 B가 세금액을 주지 않아
표준이 불완전해져 탈락. 딸린 결정으로, 조식 포함 여부는 "요금의 조건"이므로 가격 정보와 함께 두어 조건
차이가 드러나게 하고, 금액 타입은 통화 최소 단위 정수(`Long`)를 추천했다(스펙이 최소 단위 정수를 주고
연산이 합산뿐이라 부동소수점 오차 여지가 없음).

**결정(수용 + 보강)**: **표준 요금 = { 세금 포함 총액(Long), 통화, 조식 포함 여부 }.** 사용자가 고객
관점의 논거를 보강했다 — 고객에게 중요한 것은 하루에 얼마인지가 아니라 "전체 내가 내는 금액이 그래서
얼마인가"이다. 조식 포함 여부는 항상 요금과 같이 다니게 한다. 금액은 `Long`으로 하되 **현재 스펙은 KRW만
고려**하고, 추후 환율이 들어가는 시점에 `BigDecimal` + 반올림 정책을 추가하기로 범위를 명확히 했다.

### 21:27 · 도메인 모델 논의 ③ — 내부 식별자 전략

**제안(AI)**: 공급사 식별자는 유일성 범위가 제각각이라(숙소는 공급사 안에서만, 객실 타입은 숙소 안에서만
유일) 그대로 쓸 수 없다. DB 대리키(추천) vs 결정적 합성키(`"A:A-10023"`)를 비교 — 대리키는 공급사 코드가
내부 식별자에 새어 나가지 않는 캡슐화 경계를 만들고, "같은 상품 → 같은 id"를 자연키 UNIQUE 제약 + 멱등
upsert로 DB 수준에서 보장한다. 합성키는 단순하지만 내부 식별자가 공급사 코드의 재포장이라 캡슐화가 약하고,
어차피 매핑 테이블이 필요한 구조라 대리키의 추가 비용이 사실상 없다.

사용자가 **키 타입까지 고려 범위를 넓혀** bigint auto_increment vs UUID/ULID 등을 비교했다(수정 요청).
UUIDv4는 랜덤 삽입이 InnoDB 클러스터드 인덱스 지역성을 깨는 반패턴이라 우선 배제. 실질 경쟁은 bigint vs
UUIDv7/ULID(시간 정렬)인데, UUID 계열이 이기는 조건 — 다중 라이터/분산 생성, 열거 가능성이 위험한 공개
식별자 — 이 이 시스템에서는 성립하지 않는다(쓰기 주체는 카탈로그 동기화 하나, 검색 API가 어차피 전체
카탈로그를 공개). 남는 비교축은 단순함이며, PK와 외부 노출 식별자는 나중에 분리할 수 있어(공개용 키 컬럼
추가) 비가역 결정도 아니다.

**결정(수용)**: **DB 대리키, `BIGINT AUTO_INCREMENT` + 자연키 UNIQUE 제약** — 숙소는 `(공급사, 숙소 코드)`,
객실 타입은 `(숙소 매핑, 객실 코드)`. UUIDv7/ULID는 성립 조건(다중 라이터, 비추측성 요구)이 생기는 시점에
재검토.

### 21:39 · 도메인 모델 논의 ④ — 예약 가능 판정

**제안(AI)**: 연박은 같은 객실 타입을 체크인일~체크아웃 전날까지 매일 확보해야 성립하므로, 전 기간 예약
가능 수는 **날짜별 잔여 수의 최소값(병목)**으로 판정한다(예: 3박 잔여 `[3,1,5]` → 1). 진짜 결정은 공급사
응답이 특정 날짜를 누락했을 때다 — 엄격(누락일=재고 0 간주, 추천) vs 관대(있는 날짜만 min). 두 갈래의
차이는 어느 쪽 실패가 더 싼가로, 엄격의 실패는 기회 손실(우리 손해)이고 관대의 실패는 오버부킹(고객
피해)이라 비대칭이 명확하다. 딸린 결정으로 판정 0 상품의 노출 여부는 응답에서 제외(일반적 검색 UX)를
추천했다.

**결정(수용 + 수정)**: 병목 최소값 판정과 누락일 엄격 처리는 수용 — 데이터가 불완전한 경우는 재고가
실제로 0인지 아닌지 모르기 때문에 예약 불가가 맞다. 다만 노출 방침은 수정됐다: **"모르는 0"과 "확실한
0"을 구분한다.**
- **누락(불확실)** → 예약 불가 + **응답에서 제외**. 매진이라고 단정할 수도 없는 미확정 상태다.
- **확정 매진(모든 날짜 데이터가 있고 최소값 0)** → **매진 표시와 함께 노출**. 추후 다른 고객의 취소로
  재고가 생겼을 때 알림을 받는 기능 등을 고려하면, 확정 매진은 숨길 정보가 아니라 후속 기능의
  진입점이다.
- **1 이상** → 예약 가능으로 노출.

AI의 "0이면 일괄 제외" 제안을 사용자가 제품 관점에서 세분화한 사례 — 판정 결과가 이진(가능/불가)이 아닌
3상태(가능/확정 매진/미확정)가 되었고, 표준 모델이 이 구분을 표현해야 한다.

### 21:49 · 도메인 모델 논의 ②-보완 — 요금에 1박가 추가 (교집합 원칙의 첫 명시적 예외)

**수정 제기(사용자)**: 총액만으로는 부족하다 — 고객에게 "전체 얼마인가"만큼 "하루에 얼마인가"도 중요하다.
1박 요금을 함께 노출하되, 실측(일자별 금액)이 없는 공급사는 null 대신 총액을 박수로 나눈 값을 보여주고,
실측을 주는 공급사는 평균 금액과 일자별 실질 금액을 같이 내려주자.

**정리(AI) 후 확정된 요금 구조**:
- `totalAmount` — 세금 포함 총액 (항상). **정산·결제 금액의 기준은 어디까지나 총액.**
- `averageNightlyAmount` — 평균 1박가 (항상). 총액 ÷ 박수로 어느 공급사든 계산 가능하므로 null 없이
  채워진다. 정수(Long) 나눗셈의 끝수 처리는 **내림** (반올림 제안을 사용자가 내림으로 수정). 평균은
  표시용 파생값이라 평균×박수 ≠ 총액일 수 있음을 명시한다.
- `nightlyRates[]` — 일자별 실질 금액 {날짜, 금액}. **실측을 주는 공급사(A)만** 채우고, 없는 공급사(B)는
  비운다 — "평균을 일자별로 복제해 채우는" 대안은 실측이 아닌 값을 실측처럼 보이게 하므로 거부(사용자
  확인).
- `currency`, `breakfastIncluded` — 기존대로 요금과 동행.

**원칙과의 관계**: `nightlyRates`는 공급사에 따라 채워지는 비대칭 필드로, ①에서 정한 교집합 원칙의 **첫
명시적 예외**다. ①에서 예약해 둔 진화 조항("필요에 따라 공통 모델을 명시적으로 변경")의 첫 적용으로
기록한다. 항상 존재하는 값은 평균(일관된 UX 보장)이고 일자별은 부가 정보라는 위계를 두어 소비자 분기
부담을 최소화한다.

### 21:51 · 도메인 모델 논의 ⑤ — 중복 상품 병합은 추후 논의로 보류, 라운드 마감

**제안(AI)**: 병합하지 않음을 추천했다 — 동일성을 확정할 공통 키가 없어 추정 매칭뿐인데 오탐은 서로 다른
숙소를 하나로 합쳐 파는 사고이고, 같은 객실도 공급사별로 총액·조식 조건이 달라 합치면 정보가 왜곡된다.

**결정(보류)**: **중복 상품 병합은 지금 결정하지 않고 추후에 논의한다.** 그때까지의 동작은 식별자
전략(③)의 자연스러운 귀결 — 공급사가 다르면 같은 실제 숙소라도 별개 내부 식별자를 가지므로, 별개 상품으로
나란히 노출된다. 병합을 논의할 때는 추정 매칭의 신뢰도 관리와 조건 차이의 표현 방식이 선결 조건이다.

이로써 도메인 모델 라운드를 마감하고, ①~⑤의 결정을 `docs/DOMAIN_MODEL.md`에 반영한다.

### 22:00 · 용어 통일과 미결 목록 보강

- **용어 통일(사용자 요청)**: 문서·기록 전반의 "채널" 표현을 **"공급사"**로 통일했다(조사도 함께 교정 —
  예: "채널이" → "공급사가"). 대상: `JOURNAL.md`, `docs/DOMAIN_MODEL.md`, `docs/TECH_STACK.md`.
- **환율도 미결 목록에 등재(사용자 요청)**: 현재 범위는 KRW만 고려하며 **환율·다중 통화 처리는 추후
  논의한다**. 통화 코드는 모델에 보존되므로 데이터 유실은 없지만, 다중 통화 노출·비교가 필요해지는 시점에
  금액 타입의 `BigDecimal` 전환과 반올림 정책, 환율 소스와 기준 시점, 환산가·원통화가 표시 방식을 묶어서
  결정한다. `docs/DOMAIN_MODEL.md` 미결 섹션에 중복 상품 병합과 나란히 올렸다.

### 22:20 · 용어 라운드 — 표준 도메인 모델의 영어 용어(ubiquitous language) 확정

**제안(AI)**: 결정이 필요한 용어 7가지를 후보·추천과 함께 제시했다 — 숙소, 객실 타입, 검색 결과 상품 단위,
요금 묶음, 가용성 3상태, 매핑 엔티티/테이블 이름, 숙소 목록 개념. supplier(공급사)는 사용자가 먼저 확정.

**결정 내역**:
- **① 숙소 = `property`** (수용). 공급사 B의 원시 `propertyId`와 겹치지만 원시 용어는 어댑터 경계 안에만
  존재하므로 충돌 없음 — 용어집에 주의로 명시.
- **② 객실 타입 = `roomType`** (수용). "개별 물리 객실이 아닌 타입"임이 이름에 드러난다.
- **③ 검색 결과 단위 = `Offer`** (여러 차례 왕복 끝 확정). 처음 `offer`를 추천했으나 사용자가 보류 →
  가용성 3상태와 조회 명칭의 `availability` 중복을 사용자가 지적해 조회 명칭으로 `quote`를 제안 → 사용자가
  "굳이 워딩을 나눠야 하나"라고 문제 제기해 행위·산출물을 한 단어로 통일하기로 → `quote`는 이 도메인에서
  잘 쓰이지 않는다는 사용자 감각에 따라 **`offer`로 통일**. 조회는 "offer 조회"(fetchOffers), 단위는
  `Offer`. 공급사 원시 응답과 도메인 단위의 구분은 단어가 아니라 패키지 경계로 해결한다. 차선 `rate`는
  뜻이 요금에 붙어 있어 가용성까지 담는 단위에 좁고 `nightlyRates`와 층위가 겹쳐 배제.
- **④ 요금 묶음 = `price`** (수용). `money`는 범용 값객체, `rate`는 요율 뉘앙스라 배제.
- **⑤ 가용성 = `availability`**, 상태는 `AVAILABLE` / `SOLD_OUT` / `UNDETERMINED` (수용). "미확정"의
  의미는 `UNKNOWN`보다 `UNDETERMINED`가 정확하다.
- **⑥ 테이블명 단수** — `property`, `room_type` (사용자가 복수형을 먼저 제안 → 표준 논의: 복수(Rails·
  Laravel 기본)와 단수(JPA 기본, MySQL 샘플) 모두 확립된 관례라 단일 표준은 없고 일관성이 중요 → JPA
  기본값과의 정합을 근거로 **단수로 결정**). 엔티티 클래스는 단수 `Property`/`RoomType`과 자연 일치.
- **⑦ 숙소 목록 = `catalog`** (수용). 정적 전체 목록이라는 성격이 드러나고 offer(동적 조회)와 대비가
  명확하다. 원래 조회 명칭이던 `availability`는 3상태와의 혼동을 피해 폐기하고 offer로 흡수했다.

관례로 확정(발명 여지 없음): `checkIn`/`checkOut`/`nights`/`adults`/`children`/`maxOccupancy`/`currency`/
`totalAmount`/`averageNightlyAmount`/`nightlyRates`/`breakfastIncluded`/`remainingRooms`/`search`.

### 22:28 · 용어 재수정 — offer·catalog 폐기, StayProduct 확정

**재론(사용자)**: `offer`가 여전히 어색하다며 "우리가 파는 상품"이라는 관점에서 `StayProduct`를 제안했고,
이어서 더 근본적인 질문을 던졌다 — **catalog와 offer라는 개념을 굳이 분리해서 이름 지을 필요가 있는가?**

**검토(AI)**: 만든 이름이 밥값을 하는 기준은 "일상 언어로 풀면 길고 모호해지는 개념을 코드 곳곳에서 반복
참조할 때"다. 그 기준으로 재검토하면 둘 다 불필요했다.
- `catalog`의 실체는 "공급사의 숙소 목록"일 뿐이며, 이미 확정한 `property`/`roomType`으로 완전히
  서술된다 — 조회는 `fetchProperties()`, 동기화는 `PropertySyncService`. 오히려 `CatalogSyncService`보다
  무엇을 동기화하는지 더 직접적이다. catalog는 정적 vs 동적 대비를 보여주려던 수사였지 필요한 개념이
  아니었다.
- `offer`도 단위 이름이 `StayProduct`가 되면 조회는 "재고·요금 조회"(`fetchStayProducts`)로 서술되어 별도
  명사가 설 자리가 없다.
- "두 API의 성격이 다르다"(목록은 정적·조건 없음, 재고·요금은 동적·조건부)는 사실은 여전히 문서에 남기되,
  고유명사 없이 보통명사로 설명한다.
- `StayProduct`의 트레이드오프도 확인했다 — product의 정적(카탈로그성) 뉘앙스와 동적 스냅샷이라는 실체의
  긴장이 있지만, catalog 개념 자체를 없애면서 겹침 우려도 함께 사라졌고, 팀이 직관적으로 쓰는 말이
  이긴다는 원칙(ubiquitous language)이 더 무겁다.

**결정(수용)**: **검색 결과 단위 = `StayProduct`. `catalog`·`offer`는 용어집에서 폐기.** 22:20에 확정했던
offer는 이 결정으로 대체된다. 최종 용어집: `supplier` / `property` / `roomType` / `StayProduct` / `price` /
`availability`(AVAILABLE·SOLD_OUT·UNDETERMINED) + 관례 용어들. 코드 서술: 숙소 목록 조회
`fetchProperties()` · 매핑 동기화 `PropertySyncService` · 재고·요금 조회 `fetchStayProducts()`.

### 22:37 · ERD 라운드 — 초안 수용, 감사 컬럼 타입을 DATETIME으로 수정

**초안(AI)**: 단수 테이블 `property` / `room_type`, BIGINT 대리키 + 자연키
UNIQUE(`uk_property_supplier_code`, `uk_room_type_property_code`) + FK 제약(`fk_room_type_property`,
LAZY 연관이되 부모 id만 읽어 N+1 회피), 감사 컬럼은 DB 관리(`DEFAULT CURRENT_TIMESTAMP` / `ON UPDATE
CURRENT_TIMESTAMP`), 요금·재고는 저장하지 않고 매핑만 저장.

**결정(수용 + 수정)**: 초안은 전반 수용하되, 감사 컬럼 타입을 **`TIMESTAMP` → `DATETIME`으로 수정**(사용자).
근거 — MySQL `TIMESTAMP`는 32비트 epoch 기반이라 **2038-01-19(UTC) 한계**가 있고(8.4에도 동일),
`DATETIME`은 9999년까지 지원한다. AI가 사실 검증 후 수용. 딸린 함의 두 가지를 함께 확정했다:
- `DEFAULT/ON UPDATE CURRENT_TIMESTAMP`는 `DATETIME`에서도 동일하게 동작하므로 DB 관리 방식은 그대로.
- `DATETIME`은 세션 시간대 변환 없이 쓴 값을 그대로 저장하므로, **앱·DB 시간대를 KST(Asia/Seoul)로
  고정**하는 것이 전제가 된다 — 시간대 정합이 타입 선택보다 중요해졌다.
- 정밀도는 초 단위로 충분해 소수점(fractional seconds)은 두지 않는다.

### 22:49 · API 논의 ① — 경로와 자원 이름

**제안(AI)**: 검색 엔드포인트는 외부에 약속된 최소 계약(`GET /api/v1/stays/search` + checkIn/checkOut/
adults/children 파라미터)이 있어 경로·파라미터 이름은 고정이고, 설계 자유는 응답 쪽에 있다. URL의
`stays`는 용어집의 `StayProduct`와 철자가 다르지만 "StayProduct들의 컬렉션"의 짧은 복수형으로 읽으면
어긋남이 없다. 운영(매핑 동기화 수동 트리거) 엔드포인트는 `/internal/**` 규약 아래 이름이 결정 지점 —
`POST /internal/properties/sync`(추천, 폐기한 catalog 대신 용어집과 정합), `/internal/catalog/sync`(폐기
용어 부활이라 탈락), `/internal/sync`(무엇의 동기화인지 안 드러남). 경로의 동사 `sync`는 REST 순수주의와
어긋나지만 운영 엔드포인트는 호환성 계약 대상이 아니므로 의도의 명확함을 우선.

**결정(수용)**: 검색은 최소 계약 경로 그대로, 운영은 **`POST /internal/properties/sync`**.

### 22:52 · API 논의 ② — 검색 파라미터 검증 규칙

**제안(AI)**: `children`은 선택 + 기본값 0(성인만 검색이 다수 사용례), 검증은 날짜 형식 오류·`checkOut >
checkIn` 위반(체크아웃 미포함 규약상 0박 성립 불가)·`adults ≥ 1`·`children ≥ 0` 위반을 400으로 거부.
계약에 없는 제약(과거 날짜 거부, 최대 박수 상한)은 발명하지 않되, 상한 없이는 초장기 요청이 공급사로 그대로
나간다는 반대 급부를 명시하고 방어 상한은 필요가 확인되는 시점에 추가.

**검증 왕복(이 라운드의 핵심 과정)**: 사용자가 "`adults ≥ 1`이 계약에 적혀 있느냐"고 물어 원문을 확인한
결과 **없었다** — 계약은 인원의 분리 전달과 공급사의 수용 인원 필터(`adults + children ≤ maxOccupancy`)만
말한다. "계약에 없는 제약을 발명하지 않는다"는 스스로의 원칙과 모순이라 AI가 해당 규칙을 철회했으나,
사용자가 **법적 근거를 제시** — 국내 숙박업은 보호자(성인) 동반 없는 미성년자 투숙을 법령·업계 관행상
제한한다. 근거의 출처가 계약이 아니라 **도메인 현실(실정법·관행)**로 확인되어 규칙을 복원했다.

**결정(수용)**: `children` 선택 + 기본 0 / 400 검증: 날짜 형식 오류, `checkOut > checkIn` 위반,
**`adults ≥ 1`(도메인 규칙 — 성인 미동반 숙박 불가)**, `children ≥ 0` / 과거 날짜·박수 상한은 두지 않음
(방어 상한은 추후 필요 시). `adults ≥ 1`이 인원 합계 최소 1명을 자연히 포함한다.

### 22:55 · API 논의 ③ — 응답 구조 (StayProduct의 wire 표현)

**제안(AI)**: 계약이 요구하는 최소 정보(내부 식별자·이름, 최대 수용 인원, 예약 가능 객실 수, 출처, 요금)를
전제로 세 가지를 결정했다.
- **가용성 표현**: 중첩 객체 `availability: { status, availableRooms }`(추천) — 도메인 3상태와 1:1이고,
  매진 판단이 "0이면 매진"이라는 매직 넘버 해석이 아닌 명시적 `status`가 되어 매진 배지·알림 기능의 토대가
  된다. `UNDETERMINED`는 응답에서 제외되므로 wire에는 `AVAILABLE`/`SOLD_OUT`만 나타난다. 대안(평평한 수 +
  boolean, 수만)은 두 필드 정합을 클라이언트가 믿어야 하거나 의미가 암묵적이라 배제.
- **실측 없는 `nightlyRates`**: 빈 배열 `[]`(추천) — null 금지 결정과 일관되고 타입이 한결같아 클라이언트
  처리가 단순. 필드 생략안은 배제.
- **최상위 배열 이름**: `stayProducts`(추천) — 응답 필드는 URL보다 도메인 모델에 가까운 자리라 용어집과
  1:1 정합을 우선. `stays`(URL 정합) 대안은 배제.

**결정(수용)**: 세 가지 모두 추천안대로 — **중첩 `availability` / 빈 배열 / `stayProducts`**.

### 23:01 · API 논의 ③-수정 — 응답 구조 재정리 (혼재 제거)

**재론(사용자)**: 확정 직후 응답 예시를 다시 보고 `availability`와 `price`가 서로 혼재되어 깔끔하지 않다고
문제를 제기했다.

**진단과 정리(AI 제안 + 사용자 결정)**: 혼재는 두 가지였다.
- **금액 객체에 조건이 섞임** — `breakfastIncluded`는 돈이 아니라 상품의 조건인데 `price` 안에 있었다.
  "조식은 요금과 같이 다닌다"는 도메인 취지는 *가격을 볼 때 조건이 나란히 보인다*이지 금액 객체 내부에
  산다는 뜻이 아니므로, **상품 레벨로 이동**(A안 채택. `nightlyRates`까지 밖으로 빼는 B안은 통화와 금액이
  분리되어 배제).
- **파생 필드 중복** — 중첩 `availability { status, availableRooms }`에서 status는 숫자로부터 완전히
  유도된다(정책상 `UNDETERMINED`는 응답에서 제외되므로 0 = SOLD_OUT, 1 이상 = AVAILABLE). 같은 사실을 두
  필드로 보내면 정합을 클라이언트가 믿어야 하는 중복이라, **사용자가 status 필드 제거를 결정** — wire는
  `availableRooms` 숫자 하나가 단일 진실이다. 3상태는 도메인 내부의 판정 개념으로 유지되고(미확정 제외를
  결정), wire는 그 결과를 숫자로 직렬화한다.

**확정된 항목 구조**: 식별자·이름들, `maxOccupancy`, `supplier`, `breakfastIncluded`(상품 레벨),
`availableRooms`(0=매진), `price { totalAmount, averageNightlyAmount, nightlyRates[], currency }`.
③에서 정한 중첩 availability 결정은 이 정리로 대체된다. 도메인 모델의 `Price`에서 `breakfastIncluded`를
`StayProduct`로 옮기는 수정이 동반된다(DOMAIN_MODEL 갱신 반영 완료).

### 23:05 · API 논의 ③-보완(isAvailable) + ④ 부분 실패와 HTTP 상태

**③-보완(사용자 제안, 수용)**: 프론트 처리 편의를 위해 `isAvailable` boolean을 추가한다. 방금 제거한
`status`와 같은 파생값이라는 긴장을 명시적으로 짚었으나 — `status` enum이 상태 체계의 중복 표현이었던 것과
달리, `isAvailable`은 프론트의 가장 흔한 분기(버튼 활성/매진 배지) 하나를 위한 **의도적 편의
비정규화**이고, 생산자가 서버 하나라 정합이 어긋날 경로가 없다. 위계를 문서에 명시한다: **정보의 진실은
`availableRooms` 하나, `isAvailable`은 서버가 보장하는 편의 파생값.**

**④ 제안(AI)**: (a) 부분 실패 표현 — 구조화 목록 `errors: [{supplier, reason}]`(추천, 빈 배열이면 전체
성공. 어느 공급사가 왜 실패했는지 드러나 안내·모니터링·디버깅에 유용). 대안인 boolean 플래그와 공급사
목록만은 정보가 부족해 배제. `reason`에는 짧은 분류 문자열만 담고 내부 상세(스택 등)는 제외 — 공급사
실패는 외부 사실이라 노출해도 500의 내부 은닉 원칙과 충돌하지 않는다. (b) HTTP 상태 — **전 공급사
실패여도 200 일관**(추천): 부분과 전체는 정도 차이일 뿐인데 상태 코드가 갈리면 클라이언트 처리 경로가
둘이 된다. `stayProducts: []` + `errors` 전원 기록으로 스키마를 항상 같게 유지. 매핑이 비어 검색 대상이
없는 경우도 200 + 빈 배열(오류가 아니라 결과 없음). 대안(전체 실패 시 502/503)은 HTTP 의미론에 충실하나
공급사가 하나뿐인 구성에서 부분/전체 경계가 애매해 배제 — 이상 감지는 상태 코드가 아니라 지표로 한다.

**결정(수용)**: (a)(b) 모두 추천대로 — **구조화 `errors` 목록 + 200 일관**.

### 23:14 · API 논의 ⑤ — 오류 응답 포맷, 라운드 마감

**제안(AI)**: 400/500의 wire 포맷으로 자체 3필드 `{ status, error, message }`(추천) — 단순하고 이 규모에
충분하며 모든 오류가 같은 모양. 대안으로 RFC 9457 Problem Details(표준 준수·확장성은 있으나 type URI 관리
등 형식 비용이 과함)와 기계 코드 추가(클라이언트 분기 수요가 생기면 그때 — YAGNI)를 제시. 400은
클라이언트가 고칠 수 있으므로 구체적 사유를, 500은 내부 상세를 로그에만 남기고 불투명 메시지만.

**결정(수용)**: **자체 3필드 포맷 확정.**

이로써 API 라운드 마감 — `docs/API.md`를 라운드 결정 전체(최소 계약 경로, `/internal/properties/sync`,
검증 규칙, `stayProducts` 항목 구조와 매진 노출 예시, 가용성 노출 정책 표, 부분 실패 표현, 3필드 오류
포맷)로 전면 갱신했다. 이전 초안의 낡은 지점(조식이 price 안, 매진 제외, 폐기된 catalog 경로, 미결정
스택(Swagger·Prometheus) 참조)도 함께 정리.

### 23:17 · 연동 논의 ① — 실패 판정 통일

**제안(AI)**: 두 공급사의 실패 전달 방식이 다르다 — A는 HTTP 상태 코드(4xx/5xx), B는 장애 상황에서도 HTTP
200에 본문 `resultCode`로만 알린다. B의 `resultCode`를 확인하지 않으면 장애 응답을 정상 데이터로 처리하는
사고가 나므로 통일은 사실상 필수이고, 결정 지점은 통일의 위치와 방식이다. **어댑터가 실패를 하나의 공통
예외 타입으로 통일해 던진다** — A의 4xx/5xx, B의 `resultCode ≠ "0000"`, 무응답(타임아웃) 모두 같은 예외로
수렴시켜, 검색·오케스트레이션 계층은 실패가 어떤 모양으로 왔는지 모른 채 공급사 무관 코드 한 벌로 부분
실패를 처리한다. 여기에 **재시도 가능 여부(retryable) 분류도 어댑터의 책임**으로 얹는다 — 일시적(타임아웃·
5xx·429·B의 E500/E503/E429) vs 영구적(4xx·E400/E401)의 판단은 각 공급사의 실패 체계를 아는 어댑터만
정확히 내릴 수 있고, 이러면 재시도 정책은 공급사를 몰라도 "retryable이면 재시도" 한 줄이 된다.

**결정(수용)**: 그대로 확정.

### 23:24 · 연동 논의 ② — 타임아웃 값과 근거

**제안(AI)**: 연결과 응답의 역할을 분리하고 연결 1초 / 응답 2초를 제안했다. 연결 1초는 정상 연결(수십
ms)의 수십 배 여유를 주면서 도달 불가 공급사를 빨리 판정하기 위함, 응답 2초는 "전체 지연 = 가장 느린
공급사" 구조에서 무응답을 빨리 끊는 것과 정상 응답을 자르지 않는 것 사이의 절충값. 사용자 요청으로 업계
표준을 조사한 결과 — 표준 숫자는 없고 두 계열이 있었다. 라이브러리 기본값은 보수적(OkHttp 연결/읽기 각
10초, **Reactor Netty는 응답 타임아웃 기본값이 아예 없음** — 명시하지 않으면 무응답 공급사가 검색을
무한정 묶는다는 것이 조사로 확인됨)이고, 실무 가이드의 산정 원칙은 연결 = RTT×3 수준, 응답 = 호출자의
지연 예산에서 역산.

**결정(수정 수용)**: **연결 1초 / 응답 5초 + yml 외부화.** 사용자가 응답을 5초로 수정 — 캐시 레이어가
없는 전제에서 무한/10초는 너무 길지만, 실제 공급사들의 연결·응답 시간 분포를 모르는 현 상태에서는 관대한
쪽이 낫다는 판단. AI도 동의 — 모르는 상태에서 2초로 시작하면 정상 응답을 일상적으로 잘라 부분 실패가
상시화되는 반면, 5초의 비용은 장애 상황에서만 지불되며 서킷 브레이커가 반복 비용을 차단한다. 실측
지표(p95/p99)가 쌓이면 그 근거로 조인다. 단서: 타임아웃까지 재시도하면 최악 지연이 5초 × 시도 횟수로
불어나므로, ④ 재시도 정책에서 "타임아웃 재시도 제외 또는 1회 제한"을 이 값과 묶어 결정한다.

### 23:33 · 연동 논의 ③ — 청킹과 동시성 상한

**제안(AI)**: 재고·요금 API의 호출당 최대 50개 제약상 50개 청킹은 계약의 직접 귀결이고, 결정 지점은 동시
호출 상한이다. 상한이 없으면 숙소 수천 개 규모에서 검색 1건이 수백 개의 외부 호출을 동시에 발사해 공급사의
호출 한도(429)를 자극하고 스스로 부분 실패를 만든다. **동시성 상한 기본 16 + yml 외부화**, 상한은 공급사별
분리 대신 전체 하나로 시작(공급사가 둘인 규모에 공급사별 상한은 과함 — 한도 차이가 실제 문제로 확인되면
명시적 진화로 분리). 확장 노트로, 상한 덕에 폭주는 막지만 청크가 많아질수록 지연이 늘어나는 구조(100청크 ÷
동시 16 ≈ 7배치 순차)와 그 시점의 후속 수단(조회 대상 축소, 짧은 TTL 캐시 — 추후 논의 목록)을 문서에
남긴다.

**결정(수용)**: 그대로 확정 — **청킹 50 + 동시성 상한 16(외부화) + 확장 노트**.

### 23:59 · 연동 논의 ③-심화 — 숙소 수천 개 규모의 대응 (보류)

**문제 제기(사용자)**: 재시도 논의에 들어가기 전에 숙소가 수천 개일 때를 먼저 고민하자.

**분석(AI)**: 산수로 문제의 본질을 확인했다 — 청크 수 = 공급사 수 × ⌈N/50⌉이라 검색 지연이 N에
선형이고(숙소 5,000개 ≈ 200청크 ≈ 동시 16 기준 13배치 ≈ 6초대), **동시성 상한은 폭주(429·자원 고갈)를 막는
장치일 뿐 지연을 줄이지 못한다.** 상한을 올리면 지연 대신 429가 터지므로 탈출구가 아니다. 수단 비교 —
(a) 짧은 TTL 캐시(인기 조합의 공급사 호출 제거, 콜드 검색 한계), (b) 백그라운드 사전 수집·스냅샷(지연이
N과 무관해지는 업계 정석 구조이나 "요금·재고 저장 안 함" 원칙의 명시적 변경 동반), (c) 대상 축소(현
계약에서 불가 — 검색 조건이 날짜·인원뿐), (d) 점진 응답(단일 응답 계약 변경 필요). (a)와 (b)는 실시간성을
내주고 스냅샷으로 옮겨가는 같은 방향의 강도 차이다. 단계 로드맵(현행 → TTL 캐시 → 스냅샷 전환 → 계약
변경 시 대상 축소)과 전환 트리거(검색 p95 > 예산, 429 관측)를 제안했다.

**결정(보류)**: **캐싱이 핵심 수단이 될 사안이므로, 미결 목록의 캐시 전략 논의와 묶어 추후 재논의한다.**
오늘의 산수와 로드맵 초안은 그 논의의 출발점으로 기록에 남긴다.

## 2026-09-05

### 00:00 · 연동 논의 ④⑤ — 재시도·서킷 브레이커는 추후 구현으로 보류

**제안(AI)**: 재시도 정책으로 "즉답형 일시 오류(5xx·429·E500/E503/E429)만 재시도 1회, 고정 200ms,
타임아웃은 제외(실패 확인에 이미 5초를 쓴 뒤 또 5초를 거는 것은 비용 대비 나쁘고, 무응답 공급사가 검색을
5초 × 횟수로 묶게 됨)"를 제시했고, 서킷 브레이커가 반복 장애 비용을 끊는 역할로 이어질 예정이었다.

**결정(보류, 사용자)**: **재시도 정책과 서킷 브레이커는 선택 구현 영역이므로 추후 구현으로 뺀다.** 필수
견고성 네 가지(병렬 호출, 타임아웃, 부분 실패 허용, 실패 판정 통일)를 먼저 확실히 하는 우선순위와
일관된다. 어댑터의 retryable 분류(①)는 그대로 유지한다 — 재시도를 나중에 도입할 때 정책이 공급사를 몰라도
되게 하는 토대이고 비용이 없다. Resilience4j 채택(스택 결정)도 도입 시점의 도구로 유효하게 남는다. 오늘
논의한 정책 초안(타임아웃 재시도 제외, 1회, 200ms)은 그 시점의 출발점으로 기록에 남긴다.

### 00:03 · 연동 논의 ⑥ — 신규 공급사 추가 런북, 라운드 마감

**제안(AI)**: 지금까지의 결정들로부터 절차를 확정 — (1) `Supplier` enum에 추가, (2) 어댑터 하나
구현(책임 세 가지: 숙소 목록 → 표준 property/roomType 변환, 재고·요금 → gross 총액·날짜별 잔여·실측
`nightlyRates` 변환, 실패 → 공통 예외 + retryable 분류), (3) yml 설정과 클라이언트 구성 추가, (4) 어댑터
테스트. **여기서 끝나야 하며**, 검색·동기화·정규화 계층은 공급사 목록을 주입받아 동작하므로 수정 대상이
아니다 — 기존 계층을 고쳐야 한다면 어댑터 경계가 새고 있다는 신호로 간주한다는 문장을 런북에 명시.

**결정(수용)**: 그대로 확정. INTEGRATION 라운드 마감 — `docs/INTEGRATION.md`를 라운드 결정 전체로 갱신.

### 00:10 · 관측성 논의 ① — 지표 스택

**제안(AI)**: 이 시스템은 부분 실패가 정상 흐름이라 HTTP 상태·전체 성공률로는 이상이 보이지 않는다 —
공급사별 성공률·지연·타임아웃 비율의 분리 관측이 유일한 이상 감지 수단이고, 타임아웃 5초 등 "실측 후
조정"으로 미뤄 둔 결정들도 전부 지표를 전제로 한다. 갈래 비교: **Micrometer + Prometheus 노출(추천)** —
Micrometer는 이미 커밋된 actuator에 내장이라 한계비용이 Prometheus 레지스트리 의존성 하나뿐이고, 텍스트
포맷이라 수집기 없이도 읽을 수 있으며 수집기를 붙이면 그대로 대시보드·알람으로 이어진다. actuator 기본
metrics만(시계열·알람 불가), 로그 기반 집계(비율·분포를 로그 파이프라인에 의존하는 우회로), 외부 APM(과함)
은 배제.

**결정(수용)**: **Micrometer + Prometheus 레지스트리 채택, `/actuator/prometheus` 노출.**

### 00:13 · 관측성 논의 ② — 계측 설계

**제안(AI)**: 계측 우선 대상은 검색 핫패스의 공급사 호출(재고·요금 조회 1건 단위)이고, 숙소 목록 동기화는
저빈도 배치라 구조화 로그로 충분(지표는 필요 확인 시). 함정 하나를 짚었다 — Spring Boot의 WebClient 자동
지표(`http.client.requests`)는 HTTP 상태 기준이라 **B의 실패(HTTP 200 + resultCode)가 성공으로 집계**되므로,
어댑터의 실패 판정 결과를 아는 커스텀 타이머가 필수다. 지표명은 이전 초안의 `supplier.availability`가 용어
라운드에서 확정한 `availability`(가용성 3상태)와 겹치므로 재명명 — **`supplier.stayproducts.fetch`**
(용어집의 `fetchStayProducts`와 1:1). 태그는 `supplier`(A|B)와 `outcome`(success|failure|timeout, 서킷
도입 시 circuit_open을 명시적 진화로 추가), outcome은 어댑터 공통 예외에서 유도. 백분위 히스토그램을
활성화한다 — 타임아웃 5초를 실측 p95/p99로 조정하기로 한 결정의 전제이며, 카디널리티(공급사 2 × outcome
3)가 작아 비용 미미. JVM·톰캣 지표는 actuator 기본 제공.

**결정(수용)**: 그대로 확정.

### 00:15 · 관측성 논의 ③ — 알람 설계, 라운드 마감

**제안(AI)**: 수집기·알람 시스템 연결은 범위 밖이고, "무엇이 이상인지"의 정의(신호와 임계값)를 설계로
남긴다. 신호 네 가지 — 공급사 성공률 저하(5분 창 success 비율 < 95%, 재시도가 없는 구조라 순단도 실패로
집계됨을 감안해 100%가 아닌 95%), 응답 지연 상승(p95 > 4초 = 응답 타임아웃 5초의 80%, 대량 타임아웃의
전조이자 타임아웃 값 재검토 신호), 타임아웃 비율(5분 창 > 5%, 건당 5초씩 검색을 묶는 가장 비싼 실패라 별도
분리), 서킷 open(도입 후 즉시 — 공급사 차단은 상품 노출 축소). 검색 API 자체의 5xx율·지연은 actuator 기본
지표(`http.server.requests`)로 커버. 임계값은 전부 출발점이며 운영 분포가 쌓이면 조정한다는 단서를 달고,
대시보드는 공급사별 패널(성공률·지연·타임아웃)을 기본 단위로 잡는다.

**결정(수용)**: 그대로 확정. MONITORING 라운드 마감 — `docs/MONITORING.md`를 라운드 결정 전체로 갱신.

### 00:43 · 아키텍처 논의 ① — 레이어·패키지 구조

**제안(AI)**: 세 갈래 비교 — **기능 레이어 슬라이스(추천)**: `web / search / sync / supplier / domain /
config`, 의존 방향은 `web → search·sync → (supplier 포트 / domain)`이고 어댑터는 포트의 구현으로 바깥에
위치. 이 프로젝트는 도메인이 사실상 하나(숙박 검색)라 유스케이스 두 개(검색·동기화)를 패키지로 나누는
수준이 구조와 규모가 맞다. **DDD 4계층**은 여러 바운디드 컨텍스트가 있을 때 빛나는 구조로 도메인 하나에는
계층 디렉토리가 의식(ceremony)이 되고, **헥사고날 디렉토리 명시**는 개념은 이미 따르므로 표기의 문제일 뿐
실익이 없다. 판단 기준은 "구조가 규모를 앞서지 않는다" — 도메인이 늘어나면 그때 명시적 진화로 재편.

**결정(수용)**: **기능 레이어 슬라이스 확정.** 사용자도 도메인이 하나라 DDD 4계층은 불필요하다는 판단을
확인.

### 00:46 · 아키텍처 논의 ② — 어댑터 경계의 구조

**제안(AI)**: 핵심 질문은 "어댑터가 무엇을 반환하는가". 어댑터가 도메인 `StayProduct`를 직접 만드는 것은
불가능하고 불가능해야 한다 — `StayProduct`의 내부 식별자는 DB 매핑에서 나오는데, 어댑터가 그걸 알려면 DB에
의존해야 하고 그 순간 "외부 형식 변환"이라는 단일 책임이 무너진다. 그래서 경계를 **두 단계 변환**으로
설계: 원시 응답 → (어댑터: 형식 통일) → 중간 표준 타입(공급사 코드 기반, 형식 동일) → (정규화: 도메인화) →
`StayProduct`(내부 id + 판정 결과).
- **포트 `SupplierClient`** (supplier 패키지): `fetchProperties()` / `fetchStayProducts(조건)` 두 메서드.
  공통 예외(+retryable)도 포트 계약의 일부로 supplier 패키지 소유. 이름은 Spring 관례 `~Client` 채택.
- **중간 표준 타입** (supplier 패키지): 숙소 목록용(코드·이름·정원)과 재고·요금용(코드 + gross 총액 +
  날짜별 잔여 + 실측 일자별 + 조식) 두 벌. 구체 클래스명은 구현 PR에서 용어집과 정합하게.
- **정규화** (search 유스케이스의 컴포넌트): 매핑으로 내부 id 치환, 가용성 판정과 평균 계산을 거쳐
  `StayProduct` 생성. 판정 규칙 자체는 도메인 정책(순수 함수, domain 소속)으로 두어 Spring 없이 단위
  테스트되는 위치에 놓는다.
- 검증 기준: 공급사 C를 추가할 때 중간 타입과 정규화는 한 글자도 안 바뀌어야 한다.

**결정(수용)**: 그대로 확정.

### 00:48 · 아키텍처 논의 ③ — 숙소 목록 동기화 시점

**제안(AI)**: 검색은 매핑을 전제로 하고(재고·요금 조회가 숙소 코드 목록을 요구), 숙소 목록은 자주 바뀌지
않는 정적 콘텐츠다 — 이 두 성격이 시점을 결정한다. 갈래 비교: 검색할 때마다 조회(정적 데이터를 핫패스에서
매번 왕복 — 탈락), 기동 시 1회만(목록 변경에 재기동 필요, 기동 순간 장애면 다음 재기동까지 공백), **기동
1회 + 주기 배치 + 수동 트리거(추천)**. 조합의 구성 — 기동 시 1회로 첫 검색 전에 준비하되 **동기화 실패가
기동을 막지 않는다**(매핑이 비면 빈 결과를 줄 뿐, 앱이 떠 있어야 복구 경로가 산다 — 가용성 우선), 주기
배치는 매일 새벽 1회(주기 외부화, 신규 숙소는 하루 내 반영이면 충분), 수동 트리거는 API 라운드에서 확정한
`POST /internal/properties/sync`. 운영 규칙 두 가지 — 공급사별 실패 격리(A 실패가 B 동기화를 막지 않음),
동시 실행 직렬화(세 경로가 겹치면 upsert의 UNIQUE 경합 — 동기화는 한 번에 하나만).

**결정(수용)**: 그대로 확정.

### 00:50 · 아키텍처 논의 ④ — Mock 공급사 구성, 라운드 마감

**제안(AI)**: Mock은 정상 / 공급사 장애(A는 HTTP 503, B는 HTTP 200 + resultCode E503) / 무응답 세 상황을
재현해야 타임아웃·부분 실패·실패 판정 통일의 실동작을 보일 수 있다. 갈래 비교 — **실행형 Mock 서버(추천)**:
같은 코드베이스의 별도 패키지(본 앱 컴포넌트 스캔 밖) + 별도 실행 + **반드시 별도 포트**(같은 프로세스에
두면 자기 자신을 HTTP 호출하며 서버 스레드가 묶여 연동 문제로 오해하기 쉬운 가짜 실패가 생기는 구조적
함정), 공급사별 모드 전환 엔드포인트(normal/error/no-response)로 실행 중 장애 주입, 응답 데이터는 고정
수준으로 최소화. WireMock(의존성 추가 + 실행형 셋업이 과함)과 테스트 전용 mock만(로컬에서 띄워 눈으로
확인할 수단 부재)은 배제. 자동 테스트용 mock은 별개 용도로 테스트 도구 논의 때 확정 — 실행형은 "사람이
확인", 테스트용은 "회귀 고정".

**결정(수용)**: 그대로 확정. ARCHITECTURE 라운드 마감 — `docs/ARCHITECTURE.md`를 라운드 결정 전체로 갱신.

### 00:55 · 문서 기반 마무리 — 보류 결정 모음, 위키 목차, 루트 README

설계 라운드가 모두 끝나 문서 기반을 마감했다.
- **`docs/DESIGN_DECISIONS.md`**: 지금까지 보류·추후로 뺀 다섯 항목(재시도, 서킷 브레이커, 캐시·숙소 수천
  개 대응, 중복 상품 병합, 환율·다중 통화)을 한곳에 모았다. 각 항목에 보류의 근거와 도입 시점의 출발점이
  될 초안을 남기고, 본문이 있는 문서로 연결했다. 실제로 논의를 거친 항목만 담았다 — 논의 안 한 주제를
  미리 적어 문서가 결정을 앞서는 일을 피했다.
- **`docs/README.md`**: 완성된 8개 문서의 목차와 독자별 권장 읽기 순서로 갱신.
- **루트 `README.md`** (사용자 요청): 프로젝트 소개, 확정 스택 표, 빌드·실행 방법(JDK 21 주의 포함), 핵심
  설계 요약 6가지(교집합 모델, gross 총액 기준 요금, 대리키, 3상태 판정, 어댑터 실패 통일, 두 단계 경계)와
  근거 한 줄씩 + 문서 링크. 현재 저장소가 "설계 라운드 완료, 구현 단계적 진행 중" 상태임을 정직하게 명시 —
  아직 없는 기능을 있는 것처럼 쓰지 않았다.

### 13:41 · CI 워크플로 (PR #1) — 구현 단계 시작, PR 흐름 전환

설계 문서 기반이 완성되어 구현 단계로 전환한다. 이제부터 작업은 브랜치 + PR + CI 확인 후 머지 흐름을
따르고, JOURNAL 항목에는 해당 PR 번호를 병기한다.

첫 PR은 CI 워크플로다 — 가이드가 "PR은 CI 통과 후 머지"를 요구하는데 정작 CI가 없었으므로, 이 게이트를
실체로 만드는 것이 다른 모든 PR의 전제가 된다. 내용: main 대상 push·PR에서 Temurin 21로 `./gradlew build`
(컴파일 + 전체 테스트) 실행. 검사 항목을 로컬 완료 기준(DoD)과 일치시켜 "로컬에서 통과하면 CI에서도
통과"하게 했고, JDK는 toolchain 결정과 동일하게 21로 고정했다. (브랜치: `ci/build-workflow`)

### 13:44 · CI 로그 점검 두 건 (PR #1)

- **"Support for Java 23" 문구**: 경고가 아니라 Gradle 8.10.2 릴리스 하이라이트 배너로, Gradle 실행 JVM의
  상한이 23이라는 뜻이다. CI는 Temurin 21로 실행되어 상한 안쪽이라 조치 불필요. 함의는 이미 문서화된
  내용(로컬 셸도 `JAVA_HOME` 21 권장)과 동일하다. 래퍼 업그레이드는 실제 필요가 생길 때 별도 결정으로.
- **Node 20 지원 중단 경고(사용자 발견)**: `checkout@v4` 등 구형 액션이 타깃하는 Node 20을 GitHub이 지원
  중단했고, 지금은 러너가 강제로 Node 24에서 돌려주지만 유예가 끝나면 실패한다. 빌드 자체(Java/Gradle)와
  무관한 액션 런타임 문제라, 열려 있는 CI PR에서 바로 해결 — 최신 메이저(checkout v7, setup-java v6,
  setup-gradle v6)로 올리고 CI 재실행 통과 확인.
- **Gradle 캐시 증분 활용(사용자 요청)**: 로그로 확인한 결과 setup-gradle이 캐시를 기본으로 관리하고
  있었다(`cache-provider: enhanced`). 구조가 곧 증분이다 — main 실행이 캐시를 저장·갱신하고 브랜치 실행은
  읽기 전용으로 재사용하므로, 의존성이 바뀐 PR이 머지되면 다음 main 실행이 캐시를 새로 쓴다. 브랜치마다
  쓰기를 허용하면 캐시 한도(10GB)에서 서로를 밀어내는 낭비가 생겨 기본값이 권장 구조다. 별도 설정 없이
  성립하므로 워크플로에 의도를 주석으로만 명시했다.

### 13:50 · PR 머지 정책 — merge commit (PR #1)

**논의**: squash vs rebase 중 무엇이 좋냐는 질문에서 출발했다. 판단 기준은 스스로 세운 원칙 — "커밋은
원자적으로, 히스토리가 산출물". **squash**는 PR 전체를 커밋 하나로 뭉개 정성 들여 쪼갠 원자 커밋과 과정을
main 히스토리에서 지우므로 배제(WIP 커밋이 많은 팀에서 빛나는 방식인데 우리는 반대로 커밋을 의도적으로
설계한다). **rebase**는 원자 커밋을 보존하고 선형이지만 PR 경계의 표식이 없다. AI는 둘 중에서는 rebase를
추천하면서 merge commit(원자 보존 + PR 경계 표식, 비선형 그래프 감수)을 병기했고, **사용자가 merge
commit을 선택**했다.

**결정(수용)**: **merge commit 방식 확정.** CLAUDE.md Git 워크플로 절에 반영. PR #1부터 적용한다.

### 14:07 · PR 자동 코드 리뷰 워크플로 (PR #2)

**배경(사용자 요청)**: PR마다 Claude가 자동으로 코드 리뷰를 남기는 워크플로를 추가한다. 다른 저장소에서
운영 중인 리뷰 워크플로를 참고하되, 그대로 복사하지 않고 이 프로젝트에 맞게 조정했다.

**제안(AI)**: `anthropics/claude-code-action@v1` 기반. PR이 열리거나 갱신될 때 실행되고, 같은 PR에 연달아
푸시하면 이전 실행을 취소한다(concurrency). 리뷰는 라인 단위 인라인 코멘트 + sticky 총평 하나로 게시해
코멘트가 쌓이지 않게 했고, 허용 도구는 읽기 전용 명령으로 제한했다. 참고 원본에서 조정한 지점: ① 액션
버전을 이 저장소 표준(checkout v7)에 맞춤 ② 성능 관점을 외부 공급사 호출 맥락(타임아웃·병렬화·부분 실패
처리)으로 교체 ③ 프로젝트 일관성 관점을 신설 — CLAUDE.md와 docs/를 먼저 읽고 어댑터 경계·교집합
모델·실패 통일·용어집 위반을 지적하게 했다.

**결정(수정 수용)**: 사용자가 리뷰 관점에 **멱등성·동시성·원자성·정합성** 추가를 요청했다. 네 가지를
"동작 보장" 관점 하나로 묶고, 일반론으로 흐르지 않도록 각각에 이 프로젝트의 실제 위험 지점을 명시했다 —
동기화 재실행의 중복 생성(멱등성), 병렬 호출·동시 실행 경로의 race와 유니크 제약 충돌(동시성), 부분 저장
상태로 끝나는 실패 경로(원자성), 저장된 매핑과 응답 조립·파생 값의 불일치(정합성).

**전제 조건**: 저장소 시크릿 `CLAUDE_CODE_OAUTH_TOKEN`과 Claude GitHub App 설치가 있어야 동작한다.
시크릿 등록은 확인 완료.

### 14:10 · main 브랜치 보호 — PR 없이는 머지 불가 (사용자 요청)

저장소 ruleset으로 main(기본 브랜치)에 pull_request 규칙을 활성화했다. 이제 main에 닿는 모든 변경은
PR을 거쳐야 하며, 직접 푸시는 소유자도 차단된다(bypass 없음). 세부 판단:

- **승인 수 0**: 1인 저장소에서는 본인 PR을 본인이 승인할 수 없어, 승인을 요구하면 머지가 영구히 막힌다.
  사람 승인 대신 CI 통과 확인과 자동 리뷰(PR #2)가 게이트 역할을 한다.
- **허용 머지 방식을 merge commit 하나로 제한**: 13:50에 확정한 머지 정책을 저장소 설정에 이어 보호
  규칙에서도 강제한다.
- **`require_extra_approval_for_unattributed_changes`는 껐다**: 생성 시 기본값 true로 붙는 파라미터인데,
  발동하면 추가 승인을 요구하므로 승인자가 없는 1인 저장소에서는 머지가 막힌다. 요청 범위(PR 필수)만
  남겼다.

### 14:15 · @claude 멘션 응답 워크플로 추가 (PR #2)

자동 리뷰와 짝을 이루는 두 번째 워크플로다(사용자 확인 요청). 이슈나 PR 코멘트에서 `@claude`를 멘션하면
Claude가 응답하고, 요청에 따라 코드 수정까지 수행한다. 리뷰 지적 사항에 "@claude 이거 반영해줘"라고
답글을 다는 흐름으로 리뷰 워크플로와 연결된다. 조정한 지점: checkout을 저장소 표준(v7)에 맞추고, 코드
수정 시 CLAUDE.md 원칙과 docs/ 설계 문서를 따르라는 지시를 시스템 프롬프트에 더했다. 실행 조건이
멘션 이벤트이므로 기본 브랜치에 파일이 있어야 동작하며, 자동 리뷰와 마찬가지로 이 PR이 머지된 뒤부터
작동한다.

**부기 — 자동 리뷰 워크플로의 첫 실행 결과**: PR #2에서 review 체크가 11초 만에 성공으로 끝났는데, 실제
리뷰가 아니라 의도된 스킵이다. 액션은 보안 장치로 "실행할 워크플로 파일이 기본 브랜치의 것과 동일할
때"만 동작한다 — PR 브랜치가 allowedTools나 프롬프트를 바꿔치기해 토큰을 악용하는 것을 막는 검증이다.
워크플로를 처음 추가하는 PR은 main에 아직 파일이 없어 필연적으로 스킵되고, 머지 이후의 PR부터 실제
리뷰가 동작한다.

### 14:25 · MySQL 전환 라운드 — 구동 방식·스키마 관리·타임존 (PR #3)

H2 뼈대를 확정 스택(MySQL 8.4)으로 전환하는 라운드. 주제 세 개를 순서대로 논의했다.

**① 구동 방식(수정 수용)**: AI가 로컬은 docker compose, 테스트는 Testcontainers로 분리하는 안을
제안했다 — 테스트까지 compose 컨테이너를 공유하면 외부 상태(컨테이너 기동 여부, 이전 실행의 잔여
데이터)에 의존해 재현성이 깨지고, 반대로 개발까지 Testcontainers로 통일하면 재시작마다 DB가 새로 떠
개발 흐름이 무겁다는 근거였다. 사용자가 수용하면서 **로컬 데이터를 네임드 볼륨으로 유지**하는 조건을
더했다. 초기화는 `docker compose down -v`로만, 명시적으로 한다.

**② 스키마 관리(거부 후 파생 결정)**: AI는 Flyway(순수 SQL) + `ddl-auto: validate`를 제안했으나 — ERD의
MySQL 고유 DDL을 문자 그대로 쓸 수 있고 스키마 이력이 산출물로 남는다는 근거 — **사용자가 테이블 두 개
규모에 과잉이라 판단해 거부**하고 `ddl-auto` 관리로 확정했다(로컬 `update`, 테스트 `create`). 이 결정의
파생으로 감사 컬럼 관리 주체가 문제가 됐다: ERD에서 확정했던 DB 관리 DDL(`ON UPDATE
CURRENT_TIMESTAMP`)은 Hibernate가 생성하지 못한다. AI가 **Spring Data JPA Auditing으로 변경**(스키마와
감사의 관리 주체를 애플리케이션 한쪽으로 통일)을 제안했고 사용자가 수용했다. ERD.md의 해당 절을 함께
수정했다. `@EnableJpaAuditing`은 지금 붙이지 않는다 — 감사할 엔티티가 들어오는 도메인 PR에서 함께
간다(기능보다 앞서 가는 설정을 두지 않는다는 뼈대 원칙).

**③ 커넥션·타임존(수용)**: 시간대를 세 계층 전부 명시해 KST로 일치시켰다 — JVM(기동 시
`TimeZone.setDefault`, 테스트 JVM은 Gradle `user.timezone`), MySQL 컨테이너(`TZ`),
JDBC(`connectionTimeZone`). 하나만 어긋나면 로컬(KST 맥)에서는 재현되지 않고 CI(UTC 러너)에서만
나타나는 9시간 오프셋 버그가 되므로 암묵적 기본값을 배제했다. 커넥션 풀은 HikariCP 최대 10으로
명시했다(기본값과 같지만 결정을 드러냄) — 병목은 DB가 아니라 외부 공급사 호출이라 조정할 근거가 없고,
조정 시점은 지표가 알려준다. 로컬 자격 증명은 compose와 application.yml에 평문으로 두되, 로컬 컨테이너
한정 값이며 실제 환경이라면 외부 주입이 원칙임을 주석으로 남겼다.

### 14:34 · 검토 후 배제 — application-local.yml 분리 (PR #3)

DB 접속 정보 같은 환경 의존 값을 `application-local.yml`(local 프로파일)로 분리하는 안을 검토했다가
배제했다. 분리는 배포 환경이 여럿일 때 값이 갈리는 지점을 구조로 드러내는 수단인데, 이 프로젝트는 배포
없이 로컬에서만 띄우므로 프로파일 계층만 하나 늘어나는 과잉이다. 접속 정보는 `application.yml`에 그대로
두고, 로컬 한정 값이라는 사실과 실제 환경에서의 원칙(외부 주입)은 주석으로 밝힌다. 환경이 실제로
갈라지는 시점(배포 도입)이 오면 그때 분리한다.

### 14:55 · 자동 리뷰 첫 가동 — 지적 두 건 반영 (PR #3)

PR #2에서 넣은 자동 리뷰가 이 PR에서 처음 실제로 돌았다. 첫 두 실행은 Claude API 호출이 즉시
거부되며 실패했는데, GitHub 쪽 인증(워크플로 검증·App 토큰 교환·추적 코멘트)은 전부 정상이고 기존
저장소의 같은 액션은 성공 이력이 있어 저장소 시크릿의 토큰 값 문제로 좁혔다 — 토큰 재발급·재등록으로
해결(사용자). 재실행 수단이 소진되어 PR을 닫았다 다시 여는 방식(`reopened` 트리거)으로 새 실행을 걸었다.

리뷰 결론은 차단급 문제 없음. 실행 가능한 지적 두 건을 수용해 반영했다:

- **compose에 healthcheck 추가**: 최초 기동은 initdb로 수십 초가 걸려, README대로 바로 `bootRun`하면
  HikariCP 연결 실패로 기동이 실패할 수 있다는 지적. `mysqladmin ping` healthcheck를 추가하고, compose가
  실제로 준비 완료를 기다리도록 README의 안내를 `docker compose up -d --wait`로 함께 바꿨다 —
  healthcheck만 있고 `--wait`가 없으면 판정만 생기고 기다리지는 않는다.
- **포트 바인딩을 루프백으로 축소**: `3306:3306`은 모든 인터페이스에 바인딩되므로
  `127.0.0.1:3306:3306`으로 좁혔다. 로컬 전용 DB가 같은 네트워크의 다른 기기에서 보일 이유가 없다.

`ddl-auto: update`의 드리프트 한계와 `@EnableJpaAuditing` 검증 예고는 이미 문서·계획에 있는 내용의
확인이라 추가 조치 없음.

### 18:52 · Gradle 실행 JVM을 프로젝트 파일로 고정 (PR #4)

빌드마다 `JAVA_HOME`을 21로 지정해 실행하는 불편(사용자 지적)에서 출발했다. 셸 기본 java 는 25인데,
Gradle 데몬이 25에서 뜨면 Kotlin 컴파일러가 버전 문자열을 읽지 못하고 죽는 문제가 프로젝트 초기부터
있었다. `./gradlew updateDaemonJvm --jvm-version=21`로 `gradle/gradle-daemon-jvm.properties`
(`toolchainVersion=21`)를 생성해 해결했다 — toolchain 이 컴파일 대상을 고정하듯, 이 파일은 빌드를
수행하는 **데몬 JVM**을 21로 선언한다. 설치된 JDK 중 21을 자동 탐지하므로 셸·머신·CI 어디서든 동일하게
동작하고, 문제의 원인 지점(데몬)이 저장소에 커밋되는 파일로 고정된다. wrapper 클라이언트는 여전히 셸
기본 java 로 떠서 무해한 네이티브 접근 경고를 출력하지만 빌드와 무관하다. 검증: `JAVA_HOME` 지정 없이
컴파일 통과.

### 18:59 · 도메인 모델 라운드 — 구현과 엔티티 스타일 (PR #5)

설계 문서(DOMAIN_MODEL·ERD·용어집)에서 확정한 결정들을 도메인 계층 코드로 구현하는 라운드다. 모델,
엔티티, 리포지토리와 테스트를 함께 작성했다.

- **요금**: `Price`(총액 + 평균 1박가 + 일자별 실측 + 통화)와 `NightlyRate`(날짜+금액).
  `Price.of(totalAmount, nights, ...)`가 내림 나눗셈으로 평균을 계산하고, 실측이 없는 공급사는 빈
  리스트다. 조식 여부는 결정대로 `StayProduct`의 속성이다.
- **가용성**: `Availability` sealed 3상태(Available(잔여 n)/SoldOut/Undetermined)와
  `AvailabilityPolicy.judge`. "확실한 0(매진)"과 "모르는 0(미확정)"의 구분이 타입에 드러나고, 누락일이
  있으면 매진 단정도 하지 않고 Undetermined 다.
- **감사**: JPA Auditing(`@CreatedDate`/`@LastModifiedDate`) 기반 `BaseTimeEntity`와
  `@EnableJpaAuditing` — 예고대로 감사할 엔티티가 들어오는 이 라운드에서 도입했다(기능보다 앞서가지
  않기).
- **제약 이름 명시**(`uk_property_supplier_code` 등): ERD의 제약 이름과 Hibernate 생성 이름이 어긋나지
  않도록 `@Table(uniqueConstraints)`/`@JoinColumn(foreignKey)`로 명시 — PR #3 리뷰의 예고 반영.

**judge 리팩토링(사용자 주도)**: 사용자가 명령형 루프를 선언적으로 재작성하는 과정에서, 최소값을 맵
전체에서 구하면 요청 기간 밖 날짜가 판정을 오염시키는 범위 버그를 AI가 지적했다(기간 밖 날짜의 0이
매진 오판을 만든다). 최종형은 `stayDates.minOf { remainingByDate[it] ?: return Undetermined }` 한 줄로
누락 검사와 병목 계산을 합쳤고, 변수명은 계산 방식(min)이 아니라 업무 의미를 담은 `bookableRooms`로
정했다(사용자 선택). 재발 방지 테스트("기간 밖 날짜가 섞여도 판정에 영향 없음")를 추가했다.

**감사 컬럼 정밀도(결정)**: AI는 Hibernate 기본 `datetime(6)` 전환(저장 값 = 메모리 값 일치)을
추천했으나, **사용자가 ERD의 초 단위 결정 유지를 선택** — `columnDefinition = "datetime"`으로 고정하고
KDoc·ERD·구현이 일치함을 확인했다.

**엔티티 스타일(결정)**: 기존에 운영하던 코드베이스의 엔티티 스타일(전 필드 `var + protected set`,
생성자는 일반 파라미터)과 공개 자료 두 건 — [스포카 기술 블로그의 Kotlin JPA Entity
글](https://spoqa.github.io/2022/08/16/kotlin-jpa-entity.html), [위키독스의 Kotlin null 안전성
챕터](https://wikidocs.net/298257) — 을 검토했다. AI는 선별 적용(정체성 필드는 `val` 유지, 갱신 가능 필드만
`protected set`)을 제안했으나, **사용자가 전 필드 통일을 선택**했다: 생성자는 일반 파라미터로 받고
본문에서 `var + protected set` 선언, 변경은 의도가 명시된 도메인 메서드로만 연다(갱신 메서드는 호출자가
생기는 동기화 라운드에서). 검토의 부수 확인 두 가지 — 위키독스 글의 lateinit/기본값 딜레마는 no-arg
플러그인이 있는 우리 구성에서 성립하지 않고(글의 예시 코드에는 기술적 오류도 있다), 스포카 글의 ULID
PK + `Persistable` 구현은 ERD에서 기각한 결정과 충돌해 채택하지 않았다(id는 `Long = 0`으로 nullable 을
피하면서 Spring Data 신규 판정도 동작한다). 엔티티명은 **`PropertyEntity`/`RoomTypeEntity`로
확정**(사용자)하고 용어집의 엔티티 표기를 갱신했다.

**테스트**: AvailabilityPolicyTest 6건, PriceTest 7건, MappingPersistenceTest 6건. 영속화 테스트는 실제
MySQL 8.4(Testcontainers)에서 UNIQUE 제약 동작·감사 컬럼 채움·자연키 조회를 검증한다.

### 19:26 · 자동 리뷰 지적 반영 (PR #5)

리뷰 결론은 차단급 문제 없음. 실행 가능한 지적 두 건을 수용해 반영했다.

- **`Price` 생성 경로 봉인**: "평균 = 총액 ÷ 박수(내림)" 불변식이 `of()` 팩토리에서만 지켜지고 public
  생성자로 우회 가능하다는 지적. 생성자를 private 으로 막고 `@ConsistentCopyVisibility`를 함께 붙였다 —
  이것이 없으면 data class 의 `copy()`가 private 생성자를 다시 노출하는 구멍이 남는다(Kotlin 2.1).
  컴파일 통과가 "생성자·copy 를 직접 쓰는 코드가 없다"는 확인을 겸한다.
- **`MappingPersistenceTest`를 `@DataJpaTest` 슬라이스로**: 리포지토리 검증에 전체 컨텍스트를 띄우는
  것은 최소 컨텍스트 원칙 위반이라는 지적. `replace = NONE`으로 슬라이스가 datasource 를 내장 DB로
  치환하지 않고 Testcontainers 의 MySQL 을 그대로 쓰게 했다. `@EnableJpaAuditing`은 컨텍스트 루트
  (애플리케이션 클래스)에 있어 슬라이스에서도 적용된다.

엔티티 필드 검증(blank·양수) 부재 지적은 리뷰 권고대로 upsert 로직이 붙는 동기화 라운드로 이월한다.

### 21:00 · 공급사 어댑터 라운드 — 포트·중간 타입·A/B 클라이언트 (PR #6)

문서(ARCHITECTURE·INTEGRATION·용어집)에서 확정한 연동 경계를 구현하는 라운드다. 포트
`SupplierClient`(숙소 목록은 블로킹, 재고·요금은 Mono — 검색 계층의 병렬 팬아웃용), 중간 표준
타입(`SupplierProperty`/`SupplierRoomType`/`SupplierStayProduct`/`StayProductQuery`), 실패 통일
계약(`SupplierCallException` + retryable 분류), 공급사별 WebClient 구성과 A/B 어댑터로 구성된다.
원시 DTO 는 supplier/a·supplier/b 패키지에 격리되어 밖으로 새어 나가지 않는다.

구현하며 확정한 세부 결정들:

- **포트 메서드·쿼리 이름**: 재고·요금 조회는 `fetchStayProducts`/`StayProductQuery`. 용어집에서
  `availability`는 가용성 3상태의 표준어라, A 원시 엔드포인트명과 겹치는 이름을 포트 수준에 두지 않는다.
- **중간 타입의 일자별 실측 보존**: `SupplierStayProduct.nightlyAmountsByDate` — A 는 일자별
  gross(단가+세금)를 실측으로 보존하고, B 는 평균을 복제하지 않고 빈 맵이다. 도메인 `Price.nightlyRates`
  의 재료가 여기서 유실되지 않게 한다.
- **타임아웃 설정**: 연결 1초/응답 5초(INTEGRATION 결정)를 `SupplierProperties`로 바인딩. 값은
  application.yml 의 supplier 절이 **유일한 원천**이다 — 코드 기본값을 두지 않아 설정 누락이 기동 시점
  바인딩 실패로 드러난다(사용자 지적: yml 에 값이 없는 문제 → 단일 원천화로 해소).
- **MockWebServer 채택**: TECH_STACK 미결이던 외부 HTTP 검증 도구를 확정했다. 직렬화·상태 코드
  해석·무응답 타임아웃은 HTTP 를 실제로 오가야 검증되는 동작이다. 버전은 BOM 관리 밖이라 명시
  고정(4.12.0). 어댑터 테스트 12건이 실제 HTTP 왕복으로 파싱·요청 형식·실패 변환·재시도 분류를 검증한다.

### 21:10 · 어댑터 코드 리뷰 — 사용자 주도 다듬기 (PR #6)

사용자가 코드를 직접 읽으며 지적·제안하고 AI 가 반영하는 방식으로 어댑터를 다듬었다. 결정 순서대로:

- **중간 타입과 도메인 타입의 분리 유지**: `SupplierStayProduct`와 `StayProduct`를 합칠 수 있는지
  질문 → 두 타입은 원료(공급사 코드·재고 원료)와 제품(내부 id·판정 결과)의 관계라 유지로 결론.
  합치면 어댑터가 DB 와 도메인 판정을 알게 되어 두 단계 경계가 무너진다.
- **`isTimeout` 위치**: 설정 클래스(WebClientConfig)에 있던 것을 실패 계약이 있는 supplier 패키지로
  이동(사용자 지적: 설정 파일에 분류 로직은 어색하다). 인터페이스 디폴트 메서드·companion 대안을
  검토했고 일단 톱레벨 유지.
- **`@param:Qualifier`**: Kotlin 이 생성자 프로퍼티 애노테이션의 사용 지점 대상을 명시하라고 경고
  (기본 규칙이 향후 변경 예정) → 명시로 고정.
- **응답 봉투 제네릭 공통화(사용자 제안·구현)**: A 의 모든 응답이 `{items: [...]}` 봉투임을
  `SupplierABaseResponse<T>`로 표현. B 도 대칭으로 `SupplierBBaseResponse<T>`(resultCode/message/data,
  SUCCESS_CODE 는 companion). 원시 DTO 접두는 `SupplierA`/`SupplierB`로 통일(사용자 결정).
- **변환 함수 추출(사용자 제안)**: 숙소 목록 변환도 `toSupplierProperty()` 확장으로 빼서
  `toStayProduct()`와 대칭. 중첩 생성은 개행 + 네임드 인자로 매핑 관계가 드러나게(사용자 지적).
- **오류 메시지의 endpoint 특정(사용자 제안)**: 작업명 별칭("hotels") 대신 엔드포인트 경로 상수를
  `uri()`와 오류 변환이 공유 — 지어낸 어휘가 사라지고 실패 지점이 경로로 특정된다.
- **실패 변환 단일화**: 동기 경로(try/catch)와 리액티브 경로(onErrorMap)가 같은 `toSupplierError`를
  쓰도록 통일하고, A 의 "본문 없는 200 → 빈 목록" 처리도 B 처럼 계약 위반 실패로 정렬. 이후
  `toSupplierError` 자체를 supplier 패키지 공통으로 추출(사용자 지적: A/B 중복) — 전송 계층 실패
  분류는 공급사 불변 규칙이다.
- **재시도 판단 지점 단일화(사용자 제안)**: B 의 resultCode 가 HTTP 상태의 미러(E503↔503)임을 이용,
  표현만 숫자로 변환하고 판단은 공통 `isRetryableStatus`(5xx·429) 하나로. B 전용 재시도 테이블 삭제.
  가짜 HTTP 예외를 만드는 방식은 전송 사실 왜곡이라 피하고 메시지는 resultCode 원형 유지.
- **runCatching 검토 후 배제**: `runCatching`은 Error 계열까지 삼켜 JVM 치명 상태가 "공급사 실패"로
  둔갑한다. try/catch(Exception) 유지(사용자 결정).

전체 테스트 32건 통과 확인(모델 13, 어댑터 12, 영속화 6, 컨텍스트 1). Testcontainers 가 요구하는
로컬 Docker 데몬 최소 버전(25 이상)은 README 에 명시했다.

### 21:43 · 자동 리뷰 지적 반영 (PR #6)

리뷰가 실질 지적 4건을 남겼고, 전부 반영했다. 하나는 코드가 아니라 문서를 고치는 방향으로 결정됐다.

- **타임아웃 재시도 — 문서를 코드에 맞춤(사용자 결정)**: 코드는 무응답 타임아웃을 `retryable = true`로
  분류하는데 INTEGRATION 의 재시도 초안은 "타임아웃 재시도 제외"여서 상충한다는 지적. 사용자가
  **타임아웃도 재시도 대상**으로 정책을 바꾸기로 결정 — 일시 혼잡에서 회복할 기회를 버리지 않되, 비용의
  상한은 시도 횟수 제한(1회)과 서킷 브레이커가 만든다. INTEGRATION·DESIGN_DECISIONS 의 초안을 갱신했다.
- **`taxIncluded` 미사용**: 스펙 확인 결과 이 필드는 **계약상 항상 true**(세금 금액 없이 포함 사실만
  알려줌)다. `totalPrice`를 검증 없이 gross 로 신뢰하는 근거가 이 계약임을 DTO 주석으로 남겼다.
- **연결 타임아웃 미분류**: Netty 의 `ConnectTimeoutException`은 `TimeoutException`이 아니라
  ConnectException 계열이라 무응답 분기를 타지 못한다는 지적(리뷰어의 추측이 맞았다). `isTimeout`에
  추가해 연결·응답 타임아웃이 같은 "무응답" 계열로 분류되게 했다.
- **계약 위반 분기의 A/B 비대칭과 테스트 공백**: B 가 "성공 코드 + `data: null`"을 조용히 빈 리스트로
  삼키던 것을 A 의 "본문 없는 200"과 같은 계약 위반 실패로 통일했다(`requireSuccessData`) — 정상 빈
  결과는 data 안의 빈 items 로 오므로, 삼키면 깨진 응답과 진짜 빈 결과를 구분할 수 없다. 양쪽 분기에
  테스트를 추가했다(어댑터 테스트 12 → 14건).

2차 리뷰는 반영 4건을 전부 확인하고 "머지를 막을 사유 없음"으로 결론지으며 경미 2건을 남겼고, 이것도
반영했다: E 접두 없는 숫자 resultCode 가 미러 파싱을 우회하던 것을 `startsWith("E")` 확인으로 막았고,
연결 타임아웃 분류는 목 서버로 재현이 불안정해 함수 수준 단위 테스트(`SupplierErrorClassificationTest`
3건)로 회귀를 막는다는 근거를 테스트 KDoc 에 남겼다.

### 22:10 · Mock 공급사 서버 라운드 — 실행형 + 통합 테스트 (PR #7)

ARCHITECTURE 에서 확정한 실행형 Mock 을 구현하는 라운드다. A/B 의 4개 엔드포인트를 스펙 형태의 고정
픽스처로 응답하고, 공급사별 모드 전환(`/control/{supplier}/mode`)으로 실행 중에 세 상황(정상 / 장애 /
무응답)을 주입한다. 장애는 공급사의 실패 표현 방식 그대로다 — A 는 HTTP 503, B 는 200 + `resultCode`
E503. 오타 난 모드가 조용히 normal 로 동작하면 장애 주입이 안 된 채 시연하게 되므로, 알 수 없는 모드는
400 으로 즉시 거부한다. 실기동 검증: 단독 기동, 정상 픽스처 응답, 모드별 장애 주입, 2초 제한 curl 로
무응답 재현까지 확인.

**통합 테스트(사용자 요청)**: "실제 목 서버와 통신하는 테스트가 없느냐"는 질문에서 출발했다. 처음에는
"목 서버가 떠 있을 때만 실행, 없으면 스킵" 방식으로 만들었으나, **CI 와 일반 `./gradlew test` 에서도
항상 돌게 하라는 요구(사용자)**로 방식을 바꿨다 — 떠 있는 서버를 찾아가는 대신 **테스트가 Mock
애플리케이션을 임의 포트(`--server.port=0`)로 직접 기동**한다. 포트 충돌이 없어 어디서든 결정적으로
실행되고, CI 워크플로 변경도 필요 없다. 실행 로그의 "Tomcat started on port ..."로 실제 서버 기동과
실제 HTTP 왕복임을 확인했다. 이 테스트는 어댑터 회귀(MockWebServer 담당)가 아니라 **시연 자산의
보증**이 목적이다 — 실행형 Mock 픽스처와 어댑터 DTO 가 어긋나는 드리프트를 잡는다. WireMock 재검토
질문도 있었는데, 매칭·시나리오·녹화 같은 강점이 전부 우리 요구 밖이라 기존 배제를 유지했다(재시도
검증조차 MockWebServer 의 큐 방식이 더 단순하다).

### 22:29 · mock-supplier 모듈 분리 (사용자 제안, PR #7)

Mock 을 본 앱과 같은 모듈의 별도 패키지로 두는 기존 결정에는 숨은 비용이 있었다 — 본 앱 bootJar 에
Mock 코드가 실려 가고, Mock 이 본 앱 클래스패스(JPA 포함)를 물려받아 기동 시 JPA 자동 구성 제외라는
우회가 필요했다. **별도 Gradle 모듈로 분리(사용자 제안)**하여 이를 근본적으로 해결했다:

- `mock-supplier` 모듈은 웹 스타터만 의존 — DB 없이 단독 기동하고, 자동 구성 제외 3종이 통째로
  사라졌다. 실행은 `./gradlew :mock-supplier:bootRun`(9090).
- 플러그인 버전은 settings 의 `pluginManagement`로 단일 원천화 — 루트·서브모듈이 같은 버전을 쓴다.
- 통합 테스트는 본 모듈 클래스패스(JPA 포함)에서 돌므로, 목 앱 기동 시 JPA 자동 구성 제외를 **테스트
  쪽에서만** 건다 — 오염의 출처(테스트 환경)에 해결책을 둔다.
- 검증: clean 빌드 후 본 앱 jar 에 Mock 흔적 0건, 전체 테스트 37건 통과, 모듈 bootRun 시연 정상.
  ARCHITECTURE 의 "별도 패키지" 문구를 "별도 모듈"로 갱신했다.

### 23:10 · 매핑 동기화 라운드 — 갱신 정책·시점·검증 위치 (PR #8)

동기화 라운드를 논의 세 건으로 열었다.

- **갱신 정책**: 표시 속성(숙소명·객실명·정원) 변경은 재동기화에서 반영(수용). **사라진 숙소는 처리하지
  않는다(사용자)** — 스펙에 삭제·비활성 신호가 없어 일시 누락과 영구 제거를 구분할 수 없고, 검색은
  공급사 실시간 응답 기반이라 사라진 매핑은 자연히 결과에 안 나오며, 지웠다 돌아오면 내부 id 가 바뀌어
  "같은 상품 = 같은 id" 보장이 흔들린다.
- **동기화 시점(수용)**: 기동 시 1회(ApplicationRunner — 실패해도 기동은 계속, Exception 만 잡아 Error
  계열은 삼키지 않음) + 매일 새벽 4시 cron(값은 application.yml 의 `sync.property-cron`이 유일한 원천,
  기본값 없음) + 수동 트리거(`POST /internal/properties/sync`, API.md 계약).
- **엔티티 검증 위치**: "보통 어떻게 처리하나"라는 질문에 세 관례를 비교했다 — 요청 DTO Bean
  Validation(웹 입력의 지배적 관례), 외부 데이터의 경계 검증, 엔티티 생성자 불변식(DDD 성향). 동기화
  입력은 웹 요청이 아니라 첫 번째가 해당 없음을 확인했고, **엔티티 init(사용자 선택)** — Price 선례의
  "잘못된 객체는 생성 불가" 연장이고 미래의 쓰기 경로에도 구멍이 없다.

구현: `sync` 패키지(조율 + Runner/Scheduler)와 `web` 의 수동 트리거, `@EnableScheduling` 도입(스케줄러가
들어오는 이 시점 — 뼈대 원자성 원칙의 예정된 순간). 갱신은 protected set 을 여는 첫 도메인
메서드(`updateFrom`)로 반영하며, 자연키는 갱신 대상이 아니다.

### 23:30 · 동기화 코드 리뷰 — 사용자 주도 다듬기 (PR #8)

- **검증 3관문 통합(사용자 제안)**: `@PrePersist`/`@PreUpdate` 콜백 검증 제안 → 규칙 복제를 피하기 위해
  단일 `validate()`를 생성(init)·갱신(updateFrom)·저장 직전(JPA 콜백) 세 관문이 모두 호출하는 구조로
  다듬어 수용. 아는 경로에서는 즉시 터지고, updateFrom 을 우회하는 미지의 경로만 flush 그물망에 걸린다.
- **self-injection 제거(사용자 지적)**: `@Lazy self` 프록시 우회가 거슬린다는 지적 → 저장을 별도
  빈으로 분리해 해소(빈 경계를 넘으면 @Transactional 이 자연 동작하고, 조율과 저장의 책임도 갈라진다).
  이름은 Writer/WriterService/UseCase 명명 논의를 거쳐 **`PropertyMappingService`**(사용자) — 헥사고날
  UseCase 어휘는 ARCHITECTURE 에서 배제한 관례라 들이지 않았다. `@Transactional(readOnly = false)`도
  명시(사용자 — 기본값이어도 결정은 명시).
- **엔티티별 얇은 서비스 계층 검토 후 배제**: 리포지토리 2개 직접 의존 대신 각각을 감싸는 계층을 두자는
  안 → 위임만 하는 계층은 정보 없는 간접화이고(Spring Data 리포지토리가 이미 그 얇은 계층), 두 upsert
  는 한 트랜잭션의 단일 규칙이라 응집을 찢지 않기로 했다. 공유 규칙이 실제로 생기는 시점에 승격.
- upsert 두 개는 `?.apply { updateFrom(...) } ?: save(...)` 관용구로 정리(사용자 요청) — "있으면 갱신,
  없으면 생성"이 표현식 하나로 읽힌다.

### 23:45 · 동기화 실기동 검증 (PR #8)

MySQL(compose) → Mock → 본 앱 순서로 직접 기동해 전 구간을 확인했다 — 기동 로그의 공급사별 sync ok,
DB 의 매핑 3+3행과 `created_at` 의 KST 벽시계(타임존 3계층 결정의 실증), 수동 트리거의 계약 응답과 멱등
재실행까지. 부수 발견 하나: 멀티모듈에서 경로 없는 `./gradlew bootRun` 은 **모든 모듈의 bootRun 을
실행**해 떠 있는 Mock(9090)과 충돌한다 — README 실행 명령을 `:bootRun`(루트 명시)으로 고쳤다. 테스트는
10건 신설: 엔티티 불변식 6건(순수 단위), 동기화 4건(fake 공급사 + 실제 MySQL 로 멱등성·갱신 반영·부분
실패 격리·사라진 숙소 유지 — 이 라운드의 결정들이 각각 테스트로 고정됐다).

## 2026-09-06

### 00:20 · 표준 모델 재구성 — 네 단위 분리 (사용자 제안, PR #9)

스펙 요구의 "숙소·객실·요금·재고 각각 어떤 단위로 잡을지"와 모델 표면이 1:1 로 대응하는지 점검(사용자)
에서 출발했다. 기존 StayProduct 는 숙소·객실을 평면 필드로 품고 있어 요금·재고와 달리 독립 모델이
없었다. **숙소·객실을 독립 비영속 모델(`Property`/`RoomType` — 매핑 엔티티의 투영, 검증 포함)로
분리(사용자 제안)**하고 StayProduct 를 네 단위의 조합으로 재구성했다. StayProduct 를 조립하는 코드가
아직 없는 시점이라 파급이 문서와 타입 정의뿐 — 비용이 가장 싼 순간이었다. API 응답은 평면 계약을
유지하며 표현 계층에서 투영한다. DOMAIN_MODEL 에 "표준 모델의 네 단위" 절을 신설해 요구 문장과 1:1 로
대응하는 체크리스트를 만들었다.

부속 논의 — `NightlyRate`를 재고의 일부로 볼지 질문: 금액 축(요금)과 수량 축(재고)이 다르고, A 원시
응답이 둘을 한 행에 섞어 주는 것을 어댑터가 의도적으로 분리하고 있음을 확인해 요금 소속을 유지했다.

### 00:35 · SoldOut 에 확정 0 부여 — Determined 계층 (사용자 제안, PR #9)

SoldOut 도 `availableRooms = 0`을 갖자는 제안 → "판정이 확정된 상태만 수를 가진다"는 `Determined` 중간
계층으로 다듬어 수용했다. Undetermined 는 여전히 값이 없어 "모르는 0" 혼동이 타입으로 되돌아오지 않고,
검색 라운드의 웹 변환은 Determined 필터 후 수를 그대로 읽으면 된다 — "응답에 노출되는 것 = 확정된 것"이
타입 계층 그대로다. sealed interface 선택 근거도 재확인했다: 상태별로 갖는 데이터가 달라 enum + nullable
은 불법 상태를 표현 가능하게 만들고, 공유 저장 상태가 없어 sealed class 의 무게도 불필요하다.

### 00:50 · 일자별 실측 금액 제거 — 첫 예외의 번복 (사용자 결정, PR #9)

교집합 원칙의 첫 명시적 예외였던 `nightlyRates`를 제거했다. 근거(사용자): 한쪽(A)만 채우는 필드는
클라이언트에서 결국 균일하게 쓰지 못한다 — 예외 도입 때 위계(평균은 항상, 실측은 부가)로 눌렀다고 본
소비자 분기 부담이 실제로는 남는다는 판단이다. 원본이 어댑터 경계 안(A 응답)에 있으므로 복원 가능한
손실이고, 일자별 요금 표시가 실제로 필요해지는 시점에 명시적 진화로 되살린다. `Price`·중간 타입·어댑터·
API 계약·문서 5곳·테스트를 일괄 갱신했다 — 교집합 원칙이 예외 없이 성립하게 됐고, 번복의 경위는
DOMAIN_MODEL 의 "잃는 것" 목록에 남겼다. 결정을 되돌리는 것 자체가 "명시적 진화" 원칙의 실행이다.

### 01:20 · 깨진 공급사 데이터 처리 — 레코드 단위 보수 스킵과 검증 구조 확정 (PR #8)

"require 가 터지면 500으로 나가나"라는 질문에서 격리의 구멍이 드러났고, 자동 리뷰도 같은 지점을
독립적으로 지적했다 — 동기화의 공급사별 격리가 `SupplierCallException`만 잡아, 저장 단계 실패(계약 밖
데이터의 엔티티 검증 등)가 전체 동기화를 중단시키고 수동 트리거의 "항상 리스트 응답" 계약을 깬다.

**쌓을까 말까(사용자 결정: 보수 — 쌓지 않음)**: 깨진 데이터를 쌓으면 빈 이름이 검색 응답에 노출된다
(고객 피해). 쌓지 않으면 그 숙소가 검색에서 빠질 뿐이다(기회 손실). 가용성 판정과 같은 비대칭이라 같은
방향을 택했다. 단 격리 단위는 공급사가 아니라 **레코드** — 한 레코드가 공급사 전체를 무효화하지 않고,
갱신 데이터가 깨진 경우 기존의 멀쩡한 값을 유지한다. 건너뛴 수는 `skipped`로 결과·API 계약에 드러난다.

**공급사 안전망은 runCatching 으로(사용자 질문에서 확정)**: `syncSupplier`는 실패를 던지지 않고
값(결과 요약)으로 변환해 들고 다니는 자리 — 앞서 "runCatching 이 빛나는 곳"으로 정의했던 바로 그
패턴이라 `runCatching { fetch + persist }.fold(...)`로 재작성했다. Throwable 을 삼키는 결함은
`onFailure` 첫 줄의 Error 가드(`if (e !is Exception) throw e`)로 중화한다 — JVM 치명 상태를 "공급사
실패"로 포장하지 않는다. 이제 DB 오류를 포함한 어떤 실패도 공급사 단위로 격리된다.

**검증 위치의 시행착오와 최종 구조**: 스킵 구현이 검증 구조를 세 차례 흔들었다. ① 엔티티 갱신의 "할당
후 검증"이 스킵 흐름에서 관리 엔티티를 더럽히는 결함을 **테스트가 잡아냈고**(같은 트랜잭션의 후속
조회가 오염된 인스턴스를 반환), 이전 값 복원으로 땜질했다가 ② 너무 더럽다는 지적(사용자)에 값 검증
함수 분리 → 사유 반환 함수(invalidReason) 파생 구조까지 갔지만 ③ "이게 일반적이냐"는 물음에 자체 발명
미니 프레임워크임을 인정했다. 최종 확정: **경계는 표준 Bean Validation**(중간 타입에 `@NotBlank`·
`@Positive`로 공급사 계약을 선언, 동기화가 `Validator`로 저장 전 판정 — 예외 없는 스킵 흐름),
**엔티티는 `@PrePersist`/`@PreUpdate`가 붙은 단일 `validate()` 3관문**(사용자 선호 복원 — 경계가 1차를
담당하므로 여기서 터지면 버그이고 트랜잭션이 죽는 게 맞다). 규칙이 경계와 엔티티 두 층에 나타나는 것은
층위가 달라 공존하는 업계 관례로 수용했다 — 중복을 없애려는 시도가 오히려 어색함을 만들었다.

테스트 3건 추가(총 50건): 깨진 레코드 스킵, 깨진 갱신의 기존 값 유지, 예상 밖 예외의 공급사 격리.

### 14:02 · 어댑터 경계의 중복 날짜 방어 (PR #9)

자동 리뷰가 두 라운드에 걸쳐 언급한 관찰 — 같은 날짜가 두 번 오면 A 는 총액이 이중 합산되고 B 는 잔여
수가 임의의 마지막 값이 된다 — 을 방어하기로 했다(사용자). 어느 쪽이 진실인지 추측할 수 없으므로 **그
항목만 제외**하고 경고 로그를 남긴다(항목 단위 — 공급사 전체 응답은 유지, 동기화의 레코드 스킵과 같은
결). 과설계 아니냐는 재검토(사용자)에서 경계 방어의 기준을 한 줄로 확정했다: **"발생하면 조용히 틀린
돈·판정이 고객에게 나가는 경우만 경계에서 방어하고, 나머지는 계약을 신뢰한다."** 중복 날짜는 이 기준을
통과하는 마지막 항목이고, 음수 단가·null 날짜 같은 나머지 기형에는 가드를 더하지 않는다. 테스트
2건(총 52건).

### 14:40 · 검색 라운드 — 유스케이스와 웹 계층 구현 (PR #10)

스펙 핵심 흐름(매핑 로드 → 50개 청킹 → 병렬 조회 → 정규화 → 병합 → 반환)을 그대로 구현했다. 구조 결정들:

- **조회 계획의 분리**: `MappingQueryService.loadPlan()`이 readOnly 트랜잭션 하나로 매핑을 전부 읽어
  계획(보낼 코드 목록 + 응답 되번역용 lookup)으로 만들고, **원격 호출 전에 트랜잭션을 끝낸다** — DB
  커넥션이 수 초짜리 팬아웃을 가로지르지 않는다. 쿼리는 IN 절로 공급사당 정확히 2번. 비영속 표준
  모델(`Property`/`RoomType`)의 생성 지점도 엔티티를 쥔 이 트랜잭션 안 하나로 고정했다 — 직전 PR 리뷰가
  예고한 "투영이 아닌 생성 경로가 생기면 재검토"를 구조로 차단한 것.
- **미매핑 상품은 제외 + 경고 로그**: 숙소 목록에 없던 상품이 재고 응답에 오면 내부 id 를 만들 수 없다.
  즉석 매핑 생성은 거부 — 읽기 경로(검색)에 쓰기를 넣으면 트랜잭션·경합 문제를 끌어들이고, 신선도는
  수동 트리거가 이미 담당한다. 같은 이유로 숙소명 등 표시 속성도 검색 응답의 사본이 아니라 매핑을
  원천으로 쓴다.
- **부분 실패는 청크 단위 값 변환**: 각 청크 Mono 에 `onErrorResume(Exception)`을 달아 실패를
  `ChunkOutcome.Failure` 값으로 바꾼다 — flatMap 이 형제 청크를 취소시키는 에러가 흐르지 않아 부분 실패
  허용이 성립하고, 폭발 반경은 청크 하나다. Error 계열은 흡수하지 않고 전파한다(동기화 라운드에서 확정한
  구분의 리액티브 판). 같은 공급사·사유의 반복 실패는 distinct 로 병합.
- **동시 상한 16 을 yml 로 외부화**(`supplier.max-concurrent-calls`) — INTEGRATION.md 가 약속해 둔
  외부화를 이행했다. 재시도·서킷은 이 라운드에 넣지 않았다 — 초안만 문서에 있고 도입은 지표 관측 후 판단.
- **통합 테스트는 MockWebServer**: 실행형 Mock 모듈이 아니라 공급사별 MockWebServer 2대 +
  Testcontainers MySQL 로 전체 흐름(기동 동기화 → 검색 → 병합 → 투영)을 검증한다. ARCHITECTURE 에 이미
  적어 둔 역할 분담(자동 테스트 회귀 고정 = MockWebServer, 실행형 Mock = 시연·드리프트 검증)을 따른 것.
  정상 병합·503·resultCode E503·무응답 타임아웃·엄격 판정 제외·400 계열까지 9건.

### 16:30 · 응답을 표준 모델 조합 그대로 중첩 투영으로 (사용자 제안, PR #10)

평면 응답(`propertyId`, `propertyName`, ...)을 표준 모델 네 단위가 그대로 보이는
중첩(`property`·`roomType`·`availability`·`price`)으로 바꾸자는 제안을 수용했다. 모델 재구성 때 "표현
계층에서 평면으로 투영한다"고 결정했었지만, 계약 표면에서도 네 단위의 경계가 보이는 쪽이 설계 의도를 더
정직하게 드러낸다. 아직 외부 소비자가 없는 시점이라 계약 변경 비용이 0이다. `breakfastIncluded`는 도메인
결정 그대로 상품 직속을 유지했다. 거짓이 된 문서 서술(DOMAIN_MODEL 의 "평면으로 투영", ERD·API 예시)을
같은 작업에서 바로잡았다.

부속 논의 — 모양이 도메인과 거의 같은데 전용 DTO 를 유지할 가치가 있는지(사용자 질문): 유지로 결론.
sealed 3상태(`Availability`)는 어차피 순수 직렬화가 불가능해 변환 계층이 필수이고, "지금 같다"와 "같게
유지된다"는 다른 명제라 도메인 진화가 계약을 조용히 바꾸는 사고를 DTO 가 막아 준다.

### 17:10 · StayNormalizer 제거 — 치환은 lookup 으로, 조립은 유스케이스로 (사용자 지적, PR #10)

정규화를 `@Component` 빈으로 둔 것이 과하다는 지적에서 출발했다. 처음에는 분리 자체의 가치(명명된 변환
단계, 직접 단위 테스트)와 빈 격식의 무가치(상태·협력 객체 없음, 테스트조차 실물 사용)를 갈라
`object` 강등을 제안했으나, "사실상 property·roomType null 체크뿐인데 미리 스킵하면 되지 않냐"는
재반문에서 한 걸음 더 갔다: 호출 전 스킵은 불가능하지만(미매핑은 응답 안에서만 드러난다), null 체크를
조회 계획이 이미 들고 있는 `MappingLookup.resolve()`로 몰면 남는 것은 단순 조립뿐이라 **클래스 자체를
제거**했다. 조립은 서비스의 private 함수가 됐고, 판정·요금은 여전히 도메인 정책 호출이다. 테스트
커버리지는 치환·스킵 3건을 `MappingLookupTest`(순수 단위)로, 조립·미확정 유지 3건을 서비스 테스트로
이관해 유지했다.

### 17:40 · 외부 노출 메시지 한글화, 청킹 위치·상한 주입 논의 (PR #10)

- **에러 메시지 한글화**(사용자 지시): 클라이언트로 나가는 web 계층 6건(검증 3, 파라미터 누락·타입
  불일치, 500)을 한글로 바꿨다. `error` 필드는 HTTP 표준 reason phrase 라 영어 유지, 도메인 내부 require
  메시지는 로그 전용이라 유지, `errors[].reason`은 분류 문자열(HTTP 503 등)이라 유지.
- **loadPlan 에서 미리 청킹**(사용자 제안) → 거부하고 현행 유지로 합의: 호출당 50개는 매핑 데이터가
  아니라 공급사 호출 계약의 지식이라, DB 읽기 서비스로 옮기면 단일 책임이 흐려지고 청킹 검증이 순수
  단위 테스트에서 리포지토리 낀 테스트로 무거워진다.
- **호출당 상한의 공급사별 yml 주입**(사용자 제안) → 보류: 정답이 하나뿐인 계약 상수에 설정 노브를 달면
  틀릴 자유만 생긴다(엔드포인트 경로를 yml 로 빼지 않는 것과 같은 이유). 공급사별 분화가 실제로 필요해
  지는 시점에 포트 속성(`SupplierClient.maxCodesPerCall`)으로 올리기로 하고 일단 서비스 상수를 유지.

### 18:20 · 요청 검증을 선언으로 — @Valid 전환 (사용자 지시, PR #11)

컨트롤러의 수동 validate() 를 요청 DTO(`StaySearchRequest`)의 Bean Validation 선언으로 옮겼다. 400
규칙(누락 @NotNull, 하한 @Min, 날짜 형식, 교차 필드 순서 @AssertTrue)이 한 클래스의 선언으로 모이고
컨트롤러는 바인딩과 투영 두 줄만 남는다. 외부 메시지 계약은 그대로 유지했다 — 핸들러(BindException)가
위반 코드별(바인딩 실패/NotNull/선언 메시지)로 기존 문구를 조립하므로, 400 계열 통합 테스트가 수정 없이
통과한 것이 계약 보존의 증거다. CLAUDE.md 의 "컨트롤러 명시 검증 + 타입 예외" 컨벤션 문장도 같은 작업에서
갱신했다.

세부 결정 둘. **교차 필드 규칙의 기권 설계** — 날짜가 누락이면 순서 규칙(@AssertTrue)은 판정을 보류하고
통과시킨다. 누락의 400 은 @NotNull 담당이라, 겹치면 "실제 문제는 누락인데 순서가 잘못됐다는 엉뚱한
메시지"가 나갈 수 있다(표준 제약들이 null 을 통과시키는 관례와 같은 결). 이 기권이 회귀로 깨지지 않게
Validator 단위 테스트로 고정했다. **val → var** — "@NotNull 프로퍼티는 mutable 이어야 한다"는 IDE 경고는
세터 바인딩 시절의 전제가 남은 오탐이고(Spring 6.1+ 는 Kotlin 주 생성자로 바인딩해 val 로도 동작, 통합
테스트가 증명), 그래도 경고 없는 상태의 가치가 커서 var 로 전환했다. 이 DTO 는 도메인 값 객체가 아니라
바인딩 경계의 배관이며, 불변성은 검증 직후 toCriteria() 가 불변 도메인 입력으로 투영하며 회복된다.

### 18:45 · 핸들러 충분성 점검 — 404·405 가 500 으로 뭉개지는 결함 수정 (PR #11)

"핸들러가 이걸로 충분한가"라는 점검(사용자 질문)에서 결함을 찾았다. Boot 3.2+ 는 존재하지 않는 경로에
NoResourceFoundException(404), 안 맞는 메서드에 HttpRequestMethodNotSupportedException(405)을 던지는데,
catch-all(Exception → 500)이 이들을 삼켜 **클라이언트 실수가 서버 장애(500 + error 로그)로 둔갑**하고
있었다. 프레임워크가 이미 판정한 상태 코드를 보존한 채 포맷만 통일하도록 고치고, 통합 테스트 2건과
API.md 문장("클라이언트 실수를 500 으로 뭉개지 않습니다")으로 고정했다. 의도적으로 더 넓히지 않은 것:
본문 파싱 실패(본문 엔드포인트 없음)·메서드 파라미터 제약 예외(@Valid 객체 단일 경로라 도달 불가)는
필요해지는 시점에 추가한다.

### 19:05 · 핸들러 구조 재선택 — 명시적 핸들러 + ApiException 계층 유지 (사용자 결정, PR #11)

404·405 보존의 첫 구현은 catch-all 안의 ErrorResponse instanceof 분기였는데, 예외마다 핸들러 메서드를
명시적으로 나누던 이전 구조가 더 좋다는 피드백으로 재구성했다. 최종 형태: ApiException(검증 이외의
실패를 컨트롤러가 타입으로 던지는 표준 경로 — @Valid 전환으로 사용처는 없어졌지만 확장점으로 유지) /
BindException(400 조립) / NoResourceFoundException(404) / HttpRequestMethodNotSupportedException(405) /
Exception(분기 없는 순수 500). 어떤 실패가 어떤 응답이 되는지 메서드 목록만으로 드러나는 것이 채택
이유다. 트레이드오프도 기록한다 — 포괄 분기를 버렸으므로 404·405 외의 프레임워크 판정(406·415 등)은
500 으로 간다. 현재 엔드포인트 구성(콘텐츠 협상·consumes 제약 없음)에서는 도달 불가라 수용했고, 생기면
같은 스타일로 핸들러를 추가한다. 후속 정리 — 사용처가 완전히 사라진 구체 서브클래스 BadRequestException 은
제거하고 추상 확장점(ApiException)만 남겼다. 필요해지면 그때 구체 타입을 다시 세운다.

### 21:50 · 재시도·서킷 브레이커 구현 착수 (PR #12)

TECH_STACK 의 결정(Resilience4j — "Reactor retryWhen 최소주의, 서킷은 나중에"는 기각된 대안)을 이행했다.
재시도는 어댑터가 붙여 두고 아무도 소비하지 않던 `retryable` 분류를 드디어 소비한다. 적용 지점은 검색의
청크 Mono 하나 — 원격 호출에만 입혀 정규화의 내부 예외가 재시도·서킷 실패 창에 섞이지 않는다. 적용
순서는 서킷(안쪽) → 재시도(바깥쪽): 각 시도가 실패 창에 기록되고, open 이면 차단 예외를 재시도 판별이
제외해 닫힌 문을 다시 두드리지 않는다. 차단은 `errors[].reason: "circuit open"` 부분 실패가 된다.

## 2026-09-07

### 00:30 · 재시도 정책 4축 결정 (사용자 결정, PR #12)

축별 논의로 확정했다. ① 대기는 실패 종류별 차등 — 5xx·타임아웃은 짧게(200ms), 한도 초과(429/E429)는
길게(1초, 한도 창은 초 단위로 회복되므로 짧은 재시도는 한도를 더 두드릴 뿐). ② 검색 횟수는 1회 유지 —
타임아웃 5초 × 2회의 최악 대기를 고려한 지연 예산. ③ 지수 백오프(×2, 상한 5초)와 ±50% 지터 도입 —
백오프는 횟수 정책이 바뀔 수 있어 미리, 지터는 동시 실패한 병렬 청크들이 같은 시각에 재발사되어
공급사를 다시 때리는 것을 막는 현재형 장치. ④ 동기화 경로에도 적용하되 배치 성격이라 재시도 3회로
상향(사용자 응답이 아니므로 복구 확률 우선) — 서킷은 하루 1회 + 수동뿐이라 차단할 반복 호출이 없어
재시도만 입힌다.

### 01:10 · Retry-After 의 도입과 번복, yml 최대화 재구성 (사용자 결정, PR #12)

Retry-After 존중(상한 초과 시 재시도 포기 포함)까지 구현했다가 **번복하고 제거했다** — 이 공급사들은
해당 헤더를 주지 않으므로, 쓰이지 않는 해석 경로의 유지 비용만 남는다는 지적(사용자)이 맞았다. 함께
"재시도 설정을 yml 로 최대한 표현하자"는 방향이 정해졌는데, 여기서 시행착오가 있었다: ① 코드 레지스트리
빈은 자동 구성을 물러나게 해 yml 절이 조용히 무시되는 함정을 만들고, ② 공식 확장점인 커스터마이저는
yml 이 기본 대기로 심어 두는 intervalFunction 과 "둘 다 설정" 충돌로 기동이 깨졌다(빈 검증이 잡아 줌).
결론은 ③ 라이브러리 클래스를 열어 확인한 `interval-bi-function` yml 키 — 판별(retry-exception-predicate)과
같은 클래스 위임 방식이라, 시도 횟수·판별·대기 산정 연결·인스턴스 선언 전부가 yml 원천이 되고 코드에는
대기 규칙 구현(`SupplierRetryPolicy`)만 남았다. "예외별 대기 차등은 yml 로 표현 불가"라던 처음 판단은
절반만 맞았던 셈이라 정정 경위를 그대로 남긴다.

### 10:20 · 분류와 네이밍의 일원화 (사용자 지적, PR #12)

상태 기반 분류가 toSupplierError(HTTP)와 B 의 requireSuccessData(미러 코드)에 흩어져 있던 것을
`supplierErrorOfStatus` 팩토리 하나로 모았다 — 어댑터에는 "자기 실패 표현에서 상태를 추출하는 지식"만
남고, isRetryableStatus 는 private 으로 강등해 우회 조립 경로를 막았다. 부속 논의 — "B 의 E400 을 HTTP
400 인 것처럼 치환하자"는 제안은 거부했다: 판단은 이미 파생 상태로 통일되어 있고, 치환이 제거하는 것은
지식이 아니라 와이어 사실(HTTP 200 + resultCode)의 기록뿐이다. 네이밍도 일원화했다 — 재시도 설정
`search`/`sync` 와 인스턴스 `A-search`/`A-sync` 의 (공급사)-(경로) 대칭 표기를 `RetryPath` enum 하나에서
파생시켜, 경로 이름 문자열이 코드베이스 전체에서 enum 정의 한 곳에만 존재한다.

### 10:45 · 서킷 브레이커 7축 결정 (사용자 결정, PR #12)

①실패 기록은 그 공급사 호출의 모든 예외 — 서킷의 의미론을 "이 연동이 지금 동작하는가"로 잡아, 키 만료
같은 영구 실패의 반복도 차단한다(설정을 고칠 때까지 상시 open 이 정확한 동작). ②창은 최근 10회 ·
실패율 50% — 완전 장애가 현 규모에선 검색 5건 안에, 청크가 많아지면 검색 1건 안에서 감지된다. ③open
10초 — 차단이 아끼는 단위 비용(최대 ~10초)의 1배분이면서, 매진·재고 신선도가 생명인 도메인이라 복구를
빨리 알아채는 쪽. ④half-open 3회 ⑤다음 호출 트리거 — 유지. ⑥**느린 호출 기록 추가**(사용자 채택):
2초 초과가 최근 10회의 80% 이상이면 open — 검색은 가장 느린 청크에 끌려가므로 만성 저속도 병든
연동이고, 실패보다 문턱이 높은 이유는 느린 호출은 그래도 결과를 주기 때문. relaxed binding 이 오타 키를
조용히 버리는 함정에 대비해 yml 값의 실제 바인딩을 고정하는 테스트를 함께 뒀다. ⑦open 시 fallback 은
없음을 문서화(사용자 결정) — 마지막 성공 결과 서빙은 저장물이 없고 신선도 오차 허용 결정을 동반하므로
캐시 전략 논의로 연결.

### 11:00 · 500 재시도 논쟁과 명시 집합, 타임아웃 경로 분리 (사용자 제기·결정, PR #12)

"503 은 약속된 일시성이지만 500 은 아니다"라는 문제 제기에서 출발해, 기대값 논거로 **500 재시도
유지**를 결정했다: 실무 500 의 상당수는 인스턴스 단위 순단이라 재시도가 청크(숙소 50개)를 살리고,
결정적 500 의 하방은 1회 제한 + 서킷으로 유계다. 대신 분류를 범위식(>= 500)에서 **명시 집합
{429, 500, 502, 503, 504}** 로 조여 501 같은 결정적 5xx 가 쓸려 들어가지 않게 했다. 집합의 502·504 가
공급사 계약(400·401·429·500·503)에 없다는 재반문에는 "공급사 앞단 인프라가 만들어내는 전송 경로의
현실 — 계약에 없는 타임아웃을 다루는 것과 같은 층위"로 유지를 결정했다. 마지막으로 응답 타임아웃을
경로별로 분리했다(사용자 제안) — 검색 5초(고객 대기) / 동기화 10초(배치 + 큰 목록 payload). 구현은
WebClient 기본을 짧은 쪽으로 두고 동기화가 요청 단위로 덮어쓴다 — 덮어쓰기를 잊어도 "배치가 짧게
끊기는" 쪽으로 틀리지, "검색이 10초를 기다리는" 쪽으로 틀리지 않는 안전한 기본값 방향이다.
