# CLAUDE.md — 개발 가이드

이 문서는 이 저장소에서 개발하는 모든 사람과 AI가 함께 지켜야 할 원칙과 컨벤션, 그리고 작업을 마쳤다고
판단하는 기준을 정리한 것입니다. 새로운 코드를 작성하기 전에 먼저 이 문서를 읽고, 지금 하려는 변경이 아래
원칙에 어긋나지 않는지 스스로 점검해 주세요. 프로젝트가 자라면서 도메인·연동 같은 구체 영역의 규칙이
생기면, 해당 변경과 같은 작업 안에서 이 문서에 더해 갑니다.

우리가 지향하는 바는 세 가지입니다. 첫째, 명확한 경계와 단일 책임을 지키고, 모든 변경은 검증 가능해야
합니다. 둘째, 문서와 코드는 항상 일치해야 합니다. 설계 문서에 적은 내용은 실제 코드에 반영되어야 하고,
코드가 바뀌면 문서도 함께 바뀌어야 합니다. 셋째, 선택과 집중입니다. 요구를 벗어난 기능은 넣지 않고, 하지
않기로 한 것은 그 이유를 남깁니다.

---

## 1. 실행

- **JDK 21** — Gradle toolchain이 21로 고정되어 있습니다. toolchain은 컴파일 대상만 고정할 뿐 Gradle을
  실행하는 JVM은 별개이므로, 셸의 `JAVA_HOME`도 21을 가리키게 하는 것이 안전합니다.

```bash
./gradlew build    # 컴파일 + 테스트 (작업 완료 전 반드시 통과)
./gradlew test     # 테스트만 실행
./gradlew bootRun  # 애플리케이션 실행 (기본 8080)
```

## 2. 아키텍처 원칙

### 의존성 방향 (단방향, 순환 금지)

의존은 언제나 바깥에서 안쪽으로만 향해야 합니다. 즉, 더 도메인에 가까운 안쪽 계층은 바깥쪽 계층에 의존하지
않습니다. 바깥이 안쪽에 의존할 뿐이며, 참조와 import, 타입 의존이 모두 한 방향으로만 흐르도록 유지합니다.

- `Controller`는 `Service`에 의존하지만, `Service`는 `Controller`에 의존하지 않습니다.
- `Service`는 도메인 정책이나 모델에 의존하지만, 도메인 정책과 모델은 `Service`에 의존하지 않습니다.

순환 의존은 허용하지 않습니다. A가 B에 의존한다면 B는 A에, 또는 A로 되돌아오는 어떤 경로에도 의존해서는
안 됩니다. 두 클래스가 서로를 필요로 하는 상황이 생긴다면 이는 경계가 잘못 그어졌다는 신호입니다. 이럴 때는
공통되는 부분을 더 안쪽 타입으로 추출하거나, 의존을 인터페이스(포트)로 역전시켜 방향을 바로잡습니다.

안쪽 계층이 바깥의 구현을 사용해야 하는 경우에는, 바깥이 안쪽에 정의된 인터페이스를 구현하고 안쪽은 그
인터페이스에만 의존하도록 합니다. 이것이 의존성 역전 원칙(DIP)입니다. 판단이 애매할 때는 "이 클래스를 바깥
계층 없이 단독으로 컴파일하고 테스트할 수 있는가?"를 기준으로 삼으세요. 안쪽 계층일수록 그럴 수 있어야 합니다.

### 외부 시스템의 경계

외부 시스템 연동은 포트 인터페이스 뒤에 둡니다. 외부의 원시 요청·응답 형식은 어댑터 안에 가두고, 경계
밖으로는 우리가 정의한 표준 타입만 내보냅니다. 도메인·유스케이스 계층이 외부 스펙의 형식에 의존하기
시작하면 경계가 새고 있다는 신호입니다.

### API 경로 규약

클라이언트에게 제공하는 공개 API는 버전 접두사를 붙여 `/api/v1/**` 아래에 두고, 운영·관리용 엔드포인트는
버전 없이 `/internal/**` 아래에 둡니다. 호환성을 관리해야 하는 공개 계약과 그렇지 않은 운영 도구를
경로에서부터 구분하기 위해서입니다. 공개 API의 응답 형태를 바꿔야 할 때는 기존 소비자를 깨뜨리지 않도록
새 버전을 여는 것을 원칙으로 합니다.

## 3. 코딩 컨벤션

### Kotlin

들여쓰기는 4칸을 사용하고, 클래스와 enum은 PascalCase, 함수와 변수는 camelCase, 상수는 UPPER_SNAKE로
표기합니다. 이름은 표현력 있게 짓고, 주석은 "무엇을" 하는지가 아니라 "왜" 그렇게 했는지를 설명합니다.
Kotlin다운 관용구를 선호합니다 — 리파이드 확장, `runCatching`, 엘비스 연산자(`?:`)와 널 안전 연산을
활용하고, 강제 언랩(`!!`)은 사용하지 않습니다.

### 의존성 주입과 설정

의존성은 언제나 생성자로 주입하며, 협력 객체는 기본 생성자의 `private val` 파라미터로 받습니다. 필드
주입(`@Autowired lateinit var`)이나 세터 주입은 쓰지 않습니다. 같은 타입의 빈이 여럿일 때만 `@Qualifier`로
대상을 지정합니다.

설정 값은 `@Value`를 산발적으로 주입하는 대신 `@ConfigurationProperties`를 붙인 불변 `data class`로 묶어
생성자 바인딩으로 받습니다. 관련된 값은 중첩 `data class`로 묶어 응집도를 높입니다.

환경에 따라 달라지는 값은 코드나 기본 `application.yml`에 하드코딩하지 않습니다. 기본 yml에는 로컬 실행에
맞춘 안전한 기본값만 두고, 환경별로 달라지는 값은 실행 인자나 환경 변수로 오버라이드합니다. 개인용 로컬
오버라이드는 `application-local.yml`에 두되 이 파일은 커밋하지 않습니다(`.gitignore`로 제외).

서드파티 의존성 버전은 `build.gradle.kts`에 명시적으로 고정하고, 동적 버전(`+`)은 쓰지 않습니다. Spring
Boot BOM이 관리하는 아티팩트는 버전을 생략해 BOM에 위임하고, BOM 밖 라이브러리만 직접 버전을 답니다.

### 값 객체와 엔티티

값 객체는 `data class`로 정의하고 모든 필드를 `val`로 둡니다. 생성 시 반드시 성립해야 하는 불변식은
`init { require(...) }`로 강제해, 잘못된 값의 객체가 애초에 만들어지지 못하게 합니다. 이렇게 하면 검증이
서비스 계층으로 새어 나가지 않습니다.

JPA 엔티티는 프레임워크 요구상 일반 `class`로 두되, 대리키 `id`를 제외한 필드는 가능한 한 불변으로 둡니다.
PK는 `@GeneratedValue(IDENTITY)` 대리키를 쓰고, UNIQUE와 FK 제약은 `@Table`이나
`@JoinColumn(foreignKey=...)`으로 이름을 명시합니다. 연관은 꼭 필요할 때만 두며, 단방향 조회만 필요하다면
`@ManyToOne(LAZY)`로 두고 부모의 id만 읽어(프록시를 초기화하지 않아) N+1을 피합니다.

Kotlin과 JPA를 함께 쓰기 위한 필수 플러그인은 반드시 유지해야 합니다. `kotlin("plugin.spring")`은 빈을
open으로 만들고, `kotlin("plugin.jpa")`는 no-arg 생성자를 만들며, `allOpen`은 엔티티를 open으로 만들어 LAZY
프록시를 가능하게 합니다. 이 셋 중 하나라도 빠지면 트랜잭션 프록시나 지연 로딩이 조용히 깨집니다.

### 웹, DTO, 예외

도메인 엔티티를 API에 직접 노출하지 않고, 웹 응답은 전용 DTO로 변환해 내보냅니다. 요청 검증은 요청 DTO에
선언한 Bean Validation 제약(`@Valid`)으로 수행하고, 검증 이외의 실패는 컨트롤러가 타입 예외(`ApiException`
계층)로 던지며, 둘 다 `@RestControllerAdvice` 핸들러가 일관된 응답으로 변환하게 합니다. 파라미터 누락이나
타입 불일치, 예기치 못한 예외까지 모두 한 곳에서 처리합니다.

클라이언트에 내부 구현을 흘리지 않는 것이 원칙입니다. 예기치 못한 예외(500)는 원인 메시지나 스택
트레이스를 응답에 담지 않고 불투명한 메시지만 내려보내며, 상세는 error 로그로만 남깁니다. 반대로
클라이언트가 스스로 고쳐 다시 보낼 수 있는 실패(400)에는 무엇이 잘못됐는지 사람이 읽을 수 있는 사유를
담습니다. 에러 응답의 형태는 하나로 고정하고, 필드를 즉흥적으로 늘리지 않습니다.

## 4. 동시성과 트랜잭션 규칙

`@Transactional`의 self-invocation은 금지합니다. 같은 빈 안에서 메서드를 직접 호출하면 프록시를 우회하여
트랜잭션이 적용되지 않습니다. 트랜잭션 경계가 필요하다면 별도 빈으로 분리하거나 self-proxy(`@Lazy self`)를
통해 호출하세요.

트랜잭션이 원격 I/O를 가로지르지 않도록 합니다. 외부 호출이 진행되는 동안 DB 커넥션을 붙잡고 있으면 안
되며, 읽기(readOnly)는 원격 호출을 시작하기 전에 끝냅니다. upsert는 멱등하게 만들고, 동시에 실행될 수 있는
경로는 락 등으로 UNIQUE 경합을 방지합니다. WebClient의 논블로킹 체인은 컨트롤러 경계에서만 `.block()`으로
값을 모읍니다.

## 5. 로깅 규약

로그는 SLF4J(`LoggerFactory.getLogger(javaClass)`)로 남기고, 문자열 연결 대신 파라미터
플레이스홀더(`{}`)를 씁니다. 값은 `key=value` 형태로 이어 붙여 나중에 검색·집계할 수 있게 합니다.

레벨은 일관되게 사용합니다. `error`는 예기치 못한 실패, `warn`은 흡수 가능한 실패, `info`는 수명주기
이벤트, `debug`는 진단 정보에 씁니다. 자격증명을 비롯한 민감정보는 로그에 남기지 않습니다 — 요청 헤더나
쿼리를 포함한 전체 URL, 원시 응답 본문을 통째로 찍지 말고, 추적에 필요한 식별 정보만 남깁니다.

## 6. 테스트 전략

검증 대상에 필요한 최소한의 컨텍스트만 띄웁니다. 순수한 판단 로직은 Spring 없이 빠르게 도는 단위 테스트로,
외부 HTTP 연동은 mock 서버를 상대로 한 실제 왕복으로, 전체 흐름만 `@SpringBootTest`로 검증합니다. 그리고
정상 응답을 확인하는 것으로 테스트를 끝내지 않습니다 — 실패와 무응답 같은 비정상 상황을 실제로 재현해
견고성을 증명해야 합니다.

- **네이밍**: 테스트 메서드 이름은 한글 backtick 문장으로 짓고, 가능하면 `조건 - 기대결과` 형태로 씁니다.
  한 테스트는 하나의 동작만 검증합니다.
- **설정 주입과 격리**: 실행 시점에야 정해지는 값은 `@DynamicPropertySource`로 주입합니다. 테스트 간에
  새어 나가는 공유 상태는 `@BeforeEach`에서 반드시 초기화해 비결정적 실패를 막습니다.
- **픽스처**: 공용 테스트 데이터는 테스트마다 인라인으로 흩뿌리지 말고 한곳에 모아 재사용합니다.

## 7. 문서화 규율

`README.md`에는 빌드·실행 방법과 주요 설계 의사결정, 그 근거를 담습니다. 주제별 깊은 설명은 `docs/` 아래
위키로 나눕니다. 어떤 변경이 설계에 영향을 준다면 관련 문서를 같은 작업 안에서 함께 갱신하여, 문서와
코드가 어긋난 상태를 남기지 않습니다.

### JOURNAL.md — 과정 기록 (필수)

작업을 진행하면 `JOURNAL.md`에 그 과정을 기록합니다. 항목마다 날짜와 시각을 함께 적어(`## 날짜` 아래
`### HH:MM · 제목`), 언제 어떤 판단이 있었는지 시간 순으로 따라갈 수 있게 합니다. 결과물만으로는 드러나지
않는 사고의 흐름을 남기는 것이 목적이므로, 완성된 결론만 적힌 기록보다 시행착오가 드러나는 기록이 더 가치
있습니다. 단계별로 남길 것은 다음과 같습니다.

- **설계**: 의사결정 과정, 선택한 이유, 그리고 검토했다가 폐기한 대안과 그 이유.
- **개발**: 일자별 진행 상황, 부딪힌 기술적 문제와 해결 과정.
- **테스트**: 테스트 전략과 그 결과.

논의를 거쳐 결정한 경우에는 그 흐름이 드러나야 합니다 — 어떤 제안(선택지)이 나왔고, 그중 무엇을 그대로
수용했는지, 무엇을 수정해서 받아들였는지, 무엇을 거부했는지를 요약해 남깁니다. AI를 활용했다면 무엇을
물었고 그 답을 어떻게 수용·수정·거부했는지도 같은 방식으로 기록합니다.

## 8. 작업 완료 기준 (Definition of Done)

작업을 마쳤다고 말하기 전에 아래를 모두 만족하는지 점검하세요.

- [ ] `./gradlew build`가 통과한다(컴파일과 전체 테스트 모두).
- [ ] 새로 추가하거나 바꾼 동작에 대한 테스트가 있다(견고성 관련 변경이라면 실패 시나리오까지 포함한다).
- [ ] 실패하는 테스트를 끄거나 주석 처리하지 않았다(초록불은 원인을 고쳐서 만든다).
- [ ] `TODO`, `println`, `printStackTrace`, `!!` 같은 미완성·디버그 흔적이 남아 있지 않다.
- [ ] 설계에 영향을 준 변경이라면 관련 문서를 함께 갱신했다.
- [ ] `JOURNAL.md`에 이번 작업의 결정과 과정(폐기한 대안, 부딪힌 문제 포함)을 기록했다.
- [ ] 요청 범위를 벗어난 변경이 없고, 하지 않기로 한 선택은 근거를 남겼다.
- [ ] PR로 올려 CI가 통과하는 것을 확인하고, 원자적 커밋으로 히스토리에 과정이 드러나게 했다.

## 9. Git 워크플로와 저장소 위생

작업은 `main` 브랜치에 직접 푸시하지 않습니다. 의미 있는 단위로 브랜치를 나눠 작업하고, 그 단위로 PR을
생성한 뒤 CI(빌드와 전체 테스트)가 통과하는 것을 확인하고 나서 머지합니다. **머지는 merge commit
방식**을 씁니다 — squash는 정성 들여 쪼갠 원자 커밋을 뭉개고, rebase는 PR 경계의 표식을 남기지 않으므로,
원자 커밋을 보존하면서 PR 단위가 그래프에 드러나는 merge commit이 "히스토리가 산출물"이라는 원칙에
부합합니다.

커밋은 원자적으로 쪼갭니다. 하나의 커밋에는 하나의 논리적 변경만 담아, 그 커밋만 떼어 보아도 무엇을 왜
바꿨는지 드러나고 필요하면 단독으로 되돌릴 수 있어야 합니다. 커밋 제목은 명령형 현재시제로 무엇을 왜
바꿨는지 한 줄에 담고, 필요하면 본문에 배경과 대안을 적습니다. `feat:`, `fix:`, `refactor:`, `test:`,
`docs:`, `chore:` 같은 접두어(Conventional Commits)를 쓰면 히스토리에서 변경 성격을 빠르게 훑을 수
있습니다. 최종 결과물뿐 아니라 커밋 히스토리 자체도 하나의 산출물입니다.

공개 저장소인 만큼 위생에도 유의합니다. 저장소 이름과 코드, 문서, 커밋 메시지 어디에도 특정 회사명이나 이
작업의 성격을 드러내는 키워드를 넣지 않습니다. 실제 자격증명은 환경 변수나 배포 시크릿으로 주입하고
코드·설정·로그·커밋 메시지 어디에도 평문으로 남기지 않으며, 로컬 참고 자료나 비밀 값은 커밋하지
않습니다(`.local/`, `*.env` 등은 `.gitignore`로 제외).
